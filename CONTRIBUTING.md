# دليل المساهمة

شكراً لاهتمامك بتطوير **مساعد الردود للمستفيدين**. هذا الدليل يشرح بنية المشروع وكيفية إضافة المحتوى القانوني والاختبار قبل فتح Pull Request.

## البنية التقنية

- **بدون build step**: HTML/CSS/JavaScript (ES Modules) خام. لا يوجد bundler ولا framework.
- **البيانات مفصولة عن الكود**: كل المحتوى القانوني في ملفات `data/*.json` قابلة للتحرير مباشرةً دون لمس الكود.
- **الاختبارات**: `node:test` المدمج (بلا اعتماديات اختبار). الوحدات النقية في `js/` مغطاة في `tests/`.

## التشغيل المحلي

```bash
npm install        # لأدوات التطوير فقط (ESLint/Prettier)؛ التطبيق نفسه بلا اعتماديات
npm run serve      # خادم محلي على http://localhost:8000
npm test           # تشغيل اختبارات الوحدة
npm run lint       # فحص ESLint
npm run format     # تنسيق Prettier
```

> ⚠️ يجب استخدام خادم محلي (لا `file://`) بسبب ES Modules و`fetch()`.

## إضافة محتوى قانوني

### مادة نظامية جديدة — `data/articles.json`

```json
{
  "id": "1/57",
  "number": "المادة 1/57",
  "title": "عنوان مختصر للمادة",
  "text": "النص الكامل للمادة كما ورد في المصدر الرسمي.",
  "category": "التبليغ",
  "keywords": ["كلمة", "مرادف", "مصطلح"],
  "sourceUrl": "https://laws.boe.gov.sa/...",
  "lastVerified": "2026-07-16"
}
```

| الحقل | إلزامي | ملاحظات |
| --- | --- | --- |
| `id` | ✅ | معرّف فريد (نصّي). لا يتكرر. |
| `number` | ✅ | رقم المادة كما يُعرض. |
| `title` | ✅ | عنوان مختصر. |
| `text` | ✅ | النص الكامل. |
| `category` | ✅ | فئة تُنشئ زر تصفية تلقائياً. استخدم الفئات القائمة قدر الإمكان. |
| `keywords` | مصفوفة | تُحسّن مطابقة المادة مع رسائل المستفيدين. |
| `sourceUrl` | — | **يجب أن يكون رابط `https://` صالحاً** وإلا يُتجاهَل (لأسباب أمنية). |
| `lastVerified` | — | **حوكمة المحتوى**: تاريخ آخر تحقق من مطابقة النص للائحة السارية بصيغة `YYYY-MM-DD`. |

> **مهم (حوكمة المحتوى):** المحتوى القانوني يتقادم مع تعديلات الأنظمة. حدّث `lastVerified` عند كل مراجعة، وتحقق من مطابقة النص للمصدر الرسمي قبل الإضافة. ردٌّ رسمي يستند إلى مادة معدّلة أخطر من عدم الرد.

### نمط نية جديد — `data/intents.json` ← `patterns[]`

```json
{
  "id": "session-date",
  "label": "استفسار عن موعد جلسة",
  "keywords": ["موعد الجلسة", "وين جلستي"],
  "priority": 10,
  "boostCategories": { "الجلسات": 20, "المواعيد": 15 },
  "responseText": "نص الرد المقترح لهذه النية."
}
```

- `id`: معرّف ثابت (slug إنجليزي) — يُستخدم في المنطق بدل النص العربي.
- `label`: النص العربي المعروض في نتيجة التحليل.
- `keywords`: عبارات تُطابَق بعد التطبيع؛ عدد المطابقات × `priority` = درجة النية.
- `boostCategories`: خريطة `{ فئة: نقاط }` ترفع ترتيب مواد تلك الفئة عند اكتشاف النية.
- `responseText`: الرد المقترح — يُدرَج تلقائياً في «الرد النهائي». إضافة نية كاملة لا تتطلب أي تعديل على كود JS.

### قالب جاهز — `data/templates.json`

```json
{ "id": 9, "name": "اسم القالب", "icon": "📅", "text": "نص القالب مع [حقول] بين أقواس." }
```

النصوص بين `[ ]` تُعامَل كحقول تعبئة؛ يحدد التطبيق أول حقل تلقائياً عند اختيار القالب، ويملأ `[رقم الطلب]`/`[رقم الدعوى]` من آخر تحليل إن توفّر.

### قاموس العامية — `data/colloquial-map.json`

- `colloquialToFormal`: خريطة `"عامية": "فصحى"`. تُطبَّق مع مراعاة حدود الكلمة والبادئات (و/ف/ب/ل/ك).
- `legalTerms`: خريطة مصطلحات دارجة → مصطلح نظامي دقيق.
- `openings` / `closings`: عبارات الافتتاح والختام الرسمية.

## قبل فتح Pull Request

1. `npm run lint` — بلا أخطاء.
2. `npm test` — كل الاختبارات ناجحة.
3. تأكد من صحة أي JSON عدّلته (`node -e "JSON.parse(require('fs').readFileSync('data/الملف.json','utf8'))"`).
4. اكتب رسالة commit واضحة تصف التغيير.

سيتحقق GitHub Actions تلقائياً من صحة JSON والـ lint والاختبارات على كل PR.
