# 🔍 المراجعة الهندسية الشاملة — مساعد الردود للمستفيدين

> **تاريخ المراجعة:** يوليو 2026
> **نطاق المراجعة:** المستودع كاملاً (HTML/CSS/JS/JSON — ‏~2,950 سطراً)
> **المنهجية:** قراءة كاملة لكل ملف، فحص ثابت (static analysis)، تحقق من صحة بناء الوحدات وملفات JSON، بالإضافة إلى **اختبار تشغيل فعلي في متصفح Chromium** للتحقق من الأخطاء المشتبه بها قبل إدراجها. لم يُدرج أي خطأ دون دليل، وذُكرت درجة الثقة حيث يلزم.

---

## 1) الملخص العام عن جودة المشروع

المشروع **تطبيق ويب ثابت (Static Web App) بجودة عامة جيدة جداً نسبةً لحجمه وطبيعته**: أداة داخلية لموظفي المحاكم تعمل بالكامل في المتصفح دون خادم خلفي، ودون أي اعتماديات خارجية (صفر مكتبات، صفر build step)، وهو **قرار معماري صحيح ومناسب تماماً** لهذا النوع من الأدوات.

**التقدير العام: 7 / 10**

| الجانب | التقييم | ملاحظة |
| --- | --- | --- |
| المعمارية والبنية | ★★★★☆ | وحدات ES Modules نظيفة ومسؤوليات واضحة |
| جودة الكود والقراءة | ★★★★☆ | تسمية متسقة، دوال قصيرة، تفويض أحداث سليم |
| الأمان | ★★★☆☆ | وعي جيد بـ XSS لكن توجد ثغرة في سياق الخصائص (attributes) |
| الموثوقية | ★★★☆☆ | خطأ وظيفي مؤكد في شاشة الإدارة + كتابات localStorage غير محمية |
| الاختبارات | ☆☆☆☆☆ | لا توجد أي اختبارات |
| CI/CD | ☆☆☆☆☆ | لا يوجد أي workflow |
| التوثيق | ★★★★☆ | README ممتاز |
| إمكانية الوصول (A11y) | ★★☆☆☆ | فجوات في الـ modals والتنقل بلوحة المفاتيح |

---

## 2) نقاط القوة (ما نُفّذ باحترافية)

هذه النقاط تستحق الذكر صراحةً لأنها فوق المتوسط لمشاريع من هذا الحجم:

1. **اختيار معماري صائب:** Vanilla JS + ES Modules بلا build step ولا اعتماديات — يقلل سطح الهجوم، ويجعل النشر على GitHub Pages مباشراً، والصيانة ممكنة لأي مطور.
2. **فصل البيانات عن الكود:** المواد النظامية والنوايا والمرادفات وقاموس العامية كلها في ملفات JSON قابلة للتحرير دون لمس الكود — تصميم data-driven ممتاز (وإن كان غير مكتمل، انظر الملاحظة M-2).
3. **نموذج دمج التعديلات المحلية سليم:** `base + custom + deletedIds` عبر `Map` في `js/data.js:43` يسمح بتخصيص المواد محلياً مع إمكانية «إعادة التعيين» — تصميم صحيح منطقياً.
4. **وعي أمني حقيقي:** استخدام متسق لـ `escapeHtml()` في كل إخراج نصي، واستخدام `CSS.escape()` في `js/app.js:190`، و`rel="noopener"` على الروابط الخارجية.
5. **تفويض الأحداث (Event Delegation):** القوائم الديناميكية (المواد، القوالب، المحفوظات) تستخدم مستمعاً واحداً على الحاوية بدلاً من مستمع لكل عنصر — أداء وذاكرة سليمان.
6. **تجربة مستخدم مدروسة:** وضع داكن يحترم `prefers-color-scheme`، اختصارات لوحة مفاتيح مع شاشة مساعدة، CSS طباعة A4 كامل بترويسة وتذييل، تصميم متجاوب، RTL صحيح، تاريخ هجري بـ `ar-SA`.
7. **تدهور رشيق (Graceful Degradation):** فشل تحميل خطوط Google لا يكسر التطبيق (تحقّقنا من ذلك فعلياً في بيئة بلا إنترنت)، ونسخ الحافظة له مسار بديل.
8. **README نموذجي:** نظرة عامة، آلية عمل، تشغيل محلي مع تنبيه CORS، هيكل مشروع، اختصارات — توثيق أفضل من كثير من المشاريع الأكبر.

---

## 3) أبرز المخاطر التقنية (مرتبة)

| # | الخطر | الخطورة | الثقة |
| --- | --- | --- | --- |
| R-1 | حقن HTML عبر سياق الخصائص لأن `escapeHtml` لا يُهرّب علامات الاقتباس | **High** | مؤكد باختبار فعلي |
| R-2 | قائمة «إدارة المواد» لا تتحدث بعد الحفظ/الحذف/الاستيراد | **High** | مؤكد باختبار فعلي |
| R-3 | روابط `sourceUrl` غير مُتحقق من نظامها (scheme) — تقبل `javascript:` | Medium | مؤكد بقراءة الكود |
| R-4 | كتابات `localStorage` غير محمية من تجاوز الحصة (QuotaExceededError) | Medium | مؤكد |
| R-5 | الاستيراد يستبدل كل المواد المخصصة ويصفّر المحذوفات دون تحذير | Medium | مؤكد |
| R-6 | بيانات شخصية (هويات/جوالات/تفاصيل قضايا) مخزنة بنص صريح في متصفحات أجهزة العمل | Medium | تقييم سياقي |
| R-7 | غياب كامل للاختبارات وCI — أي تعديل مستقبلي بلا شبكة أمان | Medium | مؤكد |
| R-8 | «الرد النهائي» يُستبدل دون تحذير عند التحليل/القوالب/التحسين | Medium (UX) | مؤكد |

**لا توجد أخطاء بمستوى Critical**: التطبيق محلي بالكامل، لا يرسل بيانات لأي خادم، ولا يوجد مسار هجوم عن بُعد مباشر.

---

## 4) قائمة الأخطاء مرتبة حسب الأولوية (مع التحليل الكامل)

### 🔴 H-1: ‏`escapeHtml` غير آمن في سياق الخصائص (Attribute Context XSS)

- **الموقع:** `js/ui.js:3-7` والاستخدامات في `js/ui.js:90,96,123` و`js/admin.js:38`
- **الوصف:** الدالة تعتمد على `div.textContent → div.innerHTML`، وهذه الطريقة تُهرّب `& < >` فقط **ولا تُهرّب `"` أو `'`**. تحققنا فعلياً في المتصفح: المدخل `x" onmouseover="alert(1)` يخرج كما هو. القيم تُحقن داخل خصائص مقتبسة مثل `data-id="${escapeHtml(article.id)}"` و`href="${escapeHtml(article.sourceUrl)}"` و`data-category="..."`.
- **لماذا هي مشكلة:** أي مادة نظامية يكون `id` أو `sourceUrl` فيها محتوياً على `"` يستطيع الخروج من الخاصية وحقن خصائص أحداث (`onmouseover=...`). مصدر البيانات ليس المطور فقط: **ميزة «استيراد JSON» تسمح بتبادل ملفات المواد بين الموظفين**، فملف مستورد من زميل (أو من مصدر غير موثوق) يتحول إلى XSS مخزّن يعمل في كل مرة تُعرض فيها القائمة.
- **التأثير:** تنفيذ JavaScript تعسفي في جلسة الموظف → قراءة/تعديل الأرشيف المحفوظ (الذي قد يحوي بيانات مستفيدين)، تزوير محتوى الردود المولدة.
- **أفضل الممارسات:** التهريب يجب أن يكون مناسباً للسياق (HTML text ≠ HTML attribute ≠ URL). OWASP XSS Prevention Cheat Sheet: هرّب `& < > " '` كحد أدنى في الخصائص المقتبسة.
- **الحلول الممكنة:**
  | الحل | المزايا | العيوب |
  | --- | --- | --- |
  | (أ) تحويل `escapeHtml` إلى استبدال صريح للأحرف الخمسة (`& < > " '`) | سطر واحد، يصلح كل الاستخدامات دفعة واحدة | يبقى الاعتماد على انضباط المطور مستقبلاً |
  | (ب) بناء DOM برمجياً (`createElement` + `textContent` + `setAttribute`) بدل قوالب `innerHTML` | أمان بنيوي كامل، لا حاجة للتهريب أصلاً | إعادة كتابة أكبر لدوال العرض |
  | (ج) إضافة CSP صارم (`script-src 'self'`) كطبقة دفاع إضافية | يبطل معظم آثار الحقن حتى لو حدث | لا يمنع حقن HTML نفسه؛ مكمل لا بديل |
- **التوصية:** **(أ) فوراً** لأنها إصلاح من ثلاثة أسطر يغلق الثغرة، ثم **(ج)** كطبقة ثانية، و**(ب)** تدريجياً ضمن إعادة الهيكلة (المرحلة 3 في الخطة).

```js
export function escapeHtml(text) {
    return String(text ?? '')
        .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
}
```

---

### 🔴 H-2: قائمة «إدارة المواد» لا تتحدث بعد أي عملية (خطأ وظيفي مؤكد)

- **الموقع:** `js/admin.js` — الدالة `renderAdminList()` تُستدعى فقط من `initAdmin()` و`setAdminState()`، لكن `setAdminState` **لا يستدعيها أحد أبداً** (تُستورد في `js/app.js:39` ثم لا تُستخدم). دوال `upsertCustom` و`deleteArticle` ومعالجات الاستيراد وإعادة التعيين تستدعي `state.onChange(state)` فقط، وcallback في `js/app.js:69-79` لا يعيد رسم قائمة الإدارة.
- **الدليل:** اختبار تشغيل فعلي: أضفنا مادة جديدة من النموذج → ظهرت رسالة «تم حفظ المادة» لكن عدد عناصر القائمة بقي 30 (كان يجب أن يصبح 31). المادة تظهر فقط بعد إعادة تحميل الصفحة.
- **لماذا هي مشكلة:** الموظف يحذف مادة فيبقى يراها في القائمة، أو يضيف مادة فلا يجدها — يظن أن العملية فشلت فيكررها، وقد «يحذف» عنصراً محذوفاً أصلاً أو يضيف نسخاً مكررة من التعديلات.
- **الخطورة:** High (وظيفي، يمس الميزة الإدارية الأساسية) — **مؤكد 100%**.
- **الحلول الممكنة:**
  | الحل | المزايا | العيوب |
  | --- | --- | --- |
  | (أ) استدعاء `renderAdminList()` داخل `upsertCustom` / `deleteArticle` / import / reset | إصلاح موضعي فوري | يبقي منطق المزامنة مبعثراً |
  | (ب) جعل callback ‏`onChange` في app.js يستدعي `setAdminState(newState)` | يستغل الكود الموجود (الميت حالياً) ويوحّد نقطة المزامنة | يمر التحديث عبر ملفين |
  | (ج) إعادة هيكلة لإدارة حالة مركزية بنمط pub/sub صغير | يحل ازدواجية الحالة كلها (انظر M-1) | جهد أكبر من حجم المشكلة الآنية |
- **التوصية:** **(ب)** — سطر واحد في `app.js` يحيي الدالة الميتة ويصلح الخطأ، مع اعتماد (ج) لاحقاً ضمن إعادة الهيكلة.

---

### 🟠 M-1: ازدواجية ملكية الحالة بين `app.js` و`admin.js`

- **الوصف:** `admin.js` ينسخ الحالة (`state = { ...dataState, onChange }`) ويعدلها محلياً ثم «يعيدها» عبر callback، بينما `app.js` يحتفظ بنسخته في `app.data` — مصدرا حقيقة متوازيان يُزامنان يدوياً (وقد فشلت المزامنة فعلاً كما في H-2). كذلك منطق الدمج مكرر حرفياً: `mergeArticles` في `js/data.js:43` و`rebuildMerged` في `js/admin.js:83`.
- **الخطورة:** Medium (صيانة/موثوقية).
- **التأثير:** كل ميزة جديدة تمس المواد ستحتاج تذكّر المزامنة في مكانين؛ أخطاء H-2 ستتكرر بأشكال أخرى.
- **أفضل الممارسات:** Single Source of Truth؛ الدمج دالة نقية واحدة تُستدعى من الجميع.
- **الحلول:** (أ) تصدير `mergeArticles` من `data.js` واستيرادها في `admin.js` (إزالة التكرار فقط)؛ (ب) وحدة `store.js` صغيرة تملك الحالة وتبث تغييراتها (`subscribe/notify`) ويقرأ منها الجميع.
- **التوصية:** (أ) فوراً لأنها بلا مخاطرة، و(ب) عند أول ميزة جديدة تمس الحالة.

---

### 🟠 M-2: منطق النوايا مقسوم بين JSON والكود (اقتران خفي)

- **الموقع:** `js/analyzer.js:101-109` (رفع درجات فئات حسب نص النية بـ `topIntent.includes('موعد')`...) و`js/response.js:6-27` (سلسلة if/else على نصوص النوايا العربية).
- **الوصف:** أنماط النوايا تُعرَّف في `data/intents.json`، لكن ربط النية بالفئات المعززة وبنص الرد المقترح مكتوب صلباً في الكود ويعتمد على **مطابقة سلاسل عربية جزئية** (`includes`).
- **لماذا هي مشكلة:** إضافة نية جديدة في JSON (الغرض المعلن من فصل البيانات) لا تعطيها أي رد مقترح ولا أي تعزيز للمواد — الميزة تبدو «تعمل جزئياً» دون سبب ظاهر. كما أن المطابقة الجزئية هشة: أي نية مستقبلية تحتوي كلمة «موعد» سترث سلوك نية المواعيد.
- **الخطورة:** Medium (صيانة/قابلية توسعة).
- **الحلول:** (أ) إضافة حقلي `boostCategories: []` و`responseText: ""` لكل نمط في `intents.json` وجعل الكود عاماً يقرأهما؛ (ب) ملف JSON منفصل `responses.json` مفهرس بمعرّف النية.
- **التوصية:** **(أ)** — أبسط، ويحوّل النظام إلى data-driven كامل، ويمكّن غير المبرمجين من إضافة نوايا وردود كاملة. مع استبدال المطابقة الجزئية بمعرّف نية ثابت (slug) بدل النص العربي.

---

### 🟠 M-3: روابط المصادر تقبل أي scheme بما فيها `javascript:`

- **الموقع:** `js/ui.js:89-91` و`js/admin.js:117` (لا تحقق) و`js/admin.js:188` (الاستيراد لا يتحقق من `sourceUrl` إطلاقاً).
- **الوصف:** `href="${escapeHtml(article.sourceUrl)}"` — التهريب يمنع كسر الخاصية لكنه لا يمنع `javascript:alert(1)` كرابط يُنفذ عند النقر. حقل النموذج `type="url"` لا يفرض شيئاً لأن القراءة تتم بـ `.value` مباشرة دون `checkValidity()`.
- **الخطورة:** Medium (نفس نموذج تهديد H-1: ملفات استيراد متبادلة).
- **الحلول:** (أ) عند العرض والحفظ والاستيراد: قبول الروابط التي تبدأ بـ `https://` فقط (والأفضل: قصرها على نطاق `laws.boe.gov.sa` إن أريد التشدد)؛ (ب) التحقق عبر `new URL()` وفحص `protocol`.
- **التوصية:** (ب) بدالة تحقق واحدة `isSafeUrl()` تُستخدم في نقاط الحفظ والاستيراد والعرض الثلاث.

---

### 🟠 M-4: كتابات `localStorage` غير محمية من تجاوز الحصة

- **الموقع:** `js/storage.js:14`، `js/data.js:31,35`.
- **الوصف:** القراءات محمية بـ try/catch (ممتاز) لكن الكتابات لا. حصة `localStorage` ‏(~5MB) قابلة للنفاد فعلياً هنا لأن الأرشيف **غير محدود** ويُضاف إليه يومياً نصوص طويلة.
- **لماذا هي مشكلة:** عند امتلاء الحصة تُرمى `QuotaExceededError` غير ملتقطة → «تم حفظ الرد بنجاح ✓» قد لا تظهر أو الأسوأ: يفشل الحفظ بصمت جزئياً في مسارات أخرى، ويفقد الموظف رداً كتبه.
- **الخطورة:** Medium (موثوقية، فقدان بيانات).
- **الحلول:** (أ) تغليف `setItem` بـ try/catch مع toast واضح «امتلأت مساحة التخزين — صدّر الأرشيف واحذف القديم»؛ (ب) حد أقصى لعدد المحفوظات (مثلاً 500) مع تدوير تلقائي؛ (ج) الانتقال إلى IndexedDB (حصة أكبر بكثير).
- **التوصية:** (أ) + (ب) الآن؛ (ج) فقط إذا ثبت أن الاستخدام الفعلي يتجاوز الحصة.

---

### 🟠 M-5: الاستيراد مدمّر وصامت وتحققه سطحي

- **الموقع:** `js/admin.js:180-204`.
- **الوصف:** الاستيراد (1) يستبدل كل `customArticles` السابقة ويصفّر `deletedIds` دون أي تحذير أو تأكيد، و(2) يتحقق فقط من وجود الحقول (truthy) لا من أنواعها — كائن `id` غير نصي أو `keywords` بعناصر غير نصية يمر.
- **التأثير:** موظف خصّص مواده شهوراً ثم استورد ملفاً من زميل → تعديلاته كلها ضاعت بلا تراجع. والبيانات غير المطابقة للنوع قد تكسر الفرز (`a.number.localeCompare`) أو البحث لاحقاً.
- **الخطورة:** Medium.
- **الحلول:** (أ) `confirm()` قبل الاستبدال يوضح العواقب؛ (ب) خيار «دمج» بدل «استبدال» (المستورد يفوز عند تعارض id)؛ (ج) تحقق نوعي (`typeof a.id === 'string'`...) مع تقرير بعدد المرفوض وسببه؛ (د) نسخة احتياطية تلقائية للحالة السابقة في مفتاح `lastBackup` تتيح تراجعاً واحداً.
- **التوصية:** (أ)+(ج) كحد أدنى فوري، و(ب)+(د) في التحسين التالي — التكلفة صغيرة والحماية من فقدان البيانات كبيرة.

---

### 🟠 M-6: بيانات مستفيدين شخصية بنص صريح في متصفح جهاز العمل

- **الوصف:** الردود المحفوظة تتضمن غالباً أرقام هويات وجوالات وأرقام قضايا وأسماء. تُخزن في `localStorage` بلا تشفير وبلا انتهاء صلاحية، على أجهزة قد تكون مشتركة بين موظفين.
- **لماذا هي مشكلة:** أي شخص يجلس على الجهاز (أو أي سكربت يعمل في نفس الـ origin — انظر H-1) يقرأ الأرشيف كاملاً. في سياق جهة عدلية هذا اعتبار تنظيمي حقيقي (نظام حماية البيانات الشخصية السعودي PDPL).
- **الخطورة:** Medium (تعتمد على بيئة النشر الفعلية؛ إن كانت الأجهزة فردية ومقفلة تنخفض إلى Low). **درجة الثقة في التقييم متوسطة** لأنها تعتمد على سياسة تشغيل لا نراها — يُستحسن تأكيدها مع الجهة.
- **الحلول:** (أ) إخلاء مسؤولية في الواجهة + زر «مسح الأرشيف» سريع؛ (ب) انتهاء صلاحية تلقائي (حذف ما مضى عليه X يوماً)؛ (ج) تشفير الأرشيف بمفتاح مشتق من رمز مرور عبر Web Crypto API؛ (د) عدم حفظ الرسالة الواردة أصلاً والاكتفاء بالرد.
- **التوصية:** (أ)+(ب) عملياً وفوراً؛ (ج) فقط إن أكدت الجهة أن الأجهزة مشتركة — التشفير بكلمة مرور يضيف احتكاكاً يومياً يجب تبريره.

---

### 🟠 M-7: «الرد النهائي» يُستبدل دون تحذير

- **الموقع:** `js/app.js:181` (التحليل)، `js/app.js:217` (التحسين)، `js/app.js:326` (القوالب)، `js/app.js:282` (تحميل محفوظ).
- **الوصف:** أربعة مسارات تكتب فوق `finalOutput` مباشرة. الواجهة نفسها تقول للمستخدم «يمكنك تعديل الرد مباشرة» — ثم تمحو تعديله بنقرة تحليل أو قالب.
- **الخطورة:** Medium (UX/فقدان عمل المستخدم).
- **الحلول:** (أ) `confirm()` إذا كان الحقل غير فارغ وتغير عن آخر قيمة ولّدها التطبيق؛ (ب) «تراجع» بمستوى واحد (حفظ القيمة السابقة واستعادتها من toast «تم الاستبدال — تراجع»)؛ (ج) إلحاق بدل استبدال للقوالب.
- **التوصية:** (ب) — لا يقاطع سير العمل السريع (المطلوب الأساسي للأداة) ويحمي من الفقدان.

---

### 🟡 ملاحظات منخفضة الخطورة (Low)

| # | الملاحظة | الموقع | الأثر والتوصية |
| --- | --- | --- | --- |
| L-1 | **كود ميت:** `setAdminState` مستوردة ولا تُستدعى، `closeModal` مستوردة في app.js ولا تُستخدم، `STORAGE_KEYS` مُصدَّرة ولا يستوردها أحد | `app.js:34,39`، `data.js:76` | يضلل القارئ؛ يُحل تلقائياً مع H-2 وبإزالة الاستيرادات الزائدة |
| L-2 | الرقم المكون من 10 خانات يبدأ بـ 1/2 يُستخرج مرتين: «رقم طلب/مذكرة» و«رقم هوية» معاً | `analyzer.js:66-80` | تشويش بصري في نتيجة التحليل؛ استثنِ ما طابق نمط الهوية من نمط الطلب |
| L-3 | `detectTone` يفترض المفاتيح الأربعة فقط؛ نبرة جديدة في JSON تنتج `NaN` | `analyzer.js:47-50` | هشاشة أمام توسيع البيانات؛ هيّئ `scores` ديناميكياً من مفاتيح JSON |
| L-4 | `preview` يُلحق به `...` دائماً حتى للنصوص القصيرة، ويُقص بـ `substring` قد يكسر أزواج Unicode | `storage.js:25`، `ui.js:146` | تجميلي |
| L-5 | `id: Date.now()` قد يتصادم عند حفظين في نفس المللي ثانية | `storage.js:20` | احتمال شبه معدوم لمستخدم واحد؛ `crypto.randomUUID()` إن أردت الإحكام |
| L-6 | فشل تحميل البيانات يترك التطبيق ميتاً بلا زر إعادة محاولة (bindEvents لا يُنفذ) | `app.js:53-60` | أضف شاشة خطأ مع «إعادة المحاولة» |
| L-7 | لا يوجد favicon ولا `meta description` ولا خطوط preconnect | `index.html` | تحسينات صغيرة سريعة |
| L-8 | أنماط inline متناثرة في HTML (`style="margin-top: 1rem;"`...) | `index.html:117,143,203` | انقلها إلى classes |
| L-9 | `document.execCommand('copy')` مهجورة | `response.js:81` | مقبولة **كمسار بديل فقط** — لا إجراء الآن |
| L-10 | اختصار `Ctrl+Shift+C` يتعارض مع فتح DevTools في Chrome (أولوية للمتصفح أحياناً) | `shortcuts.js:7` | فكّر في `Ctrl+Shift+K` أو مشابه |
| L-11 | حذف فئة كانت مفلترة يُبقي الفلتر على فئة لم تعد موجودة | `app.js:296` | أعد الفلتر إلى «الكل» إذا اختفت الفئة |

---

## 5) فرص تحسين الأداء

الوضع الحالي **جيد ولا يوجد عنق زجاجة فعلي** عند حجم البيانات الحالي (30 مادة، ~375 مدخلة قاموس). النقاط أدناه استباقية:

1. **`improveLanguage` يبني ~375 كائن RegExp عند كل استدعاء** (`response.js:48-59`). عند الحجم الحالي التكلفة ميلي ثوانٍ؛ إن نما القاموس إلى آلاف المدخلات، قم بترجمة الأنماط مرة واحدة عند التحميل (خريطة مُجمّعة مسبقاً) — تحسين بسيط ومعزول.
2. **بحث المواد يُعيد التطبيع عند كل ضغطة مفتاح** (`app.js:314-318`): طبّع حقول المواد مرة واحدة عند التحميل وخزّنها (`_normalizedText`...) بدل إعادة الحساب، وأضف debounce بسيطاً (150ms) لحقلي البحث.
3. **الخطوط:** أضف `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` أو — الأفضل، انظر الأمان — استضف الخطوط ذاتياً مع `font-display: swap`.
4. **إعادة رسم القوائم كاملة بـ innerHTML** عند كل تغيير: مقبول تماماً عند هذا الحجم؛ لا تنتقل إلى حلول أعقد دون قياس فعلي.

---

## 6) فرص تحسين الأمان (ملخص إجرائي)

بالإضافة إلى H-1 وM-3 وM-5 وM-6 أعلاه:

1. **CSP كطبقة دفاع:** أضف `<meta http-equiv="Content-Security-Policy" content="default-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; img-src 'self' data:;">` — يبطل أثر معظم سيناريوهات الحقن. (تسقط استثناءات الخطوط إذا استُضيفت ذاتياً.)
2. **استضافة الخطوط ذاتياً:** أداة لجهة حكومية تسرّب عناوين IP للموظفين إلى طرف ثالث عند كل فتح — الاستضافة الذاتية تحل الخصوصية والعمل دون إنترنت معاً، وملفا Tajawal وAmiri woff2 صغيران.
3. **تحقق نوعي موحد للمواد** (دالة `validateArticle()` واحدة تُستخدم في النموذج والاستيراد) — يغلق M-3 وM-5 من نقطة واحدة.

---

## 7) فرص تحسين التصميم المعماري

1. **إتمام النموذج الـ data-driven (M-2):** نقل الردود المقترحة وتعزيزات الفئات إلى `intents.json` — أعلى أثر معماري بأقل تكلفة، ويحوّل إضافة نية جديدة من «تعديل 3 ملفات JS» إلى «تحرير JSON».
2. **معرّفات نوايا ثابتة (slugs):** `"intent": "session-date"` مع `"label": "استفسار عن موعد جلسة"` — يفصل المنطق عن النص المعروض ويقتل هشاشة `includes()`.
3. **وحدة حالة مركزية صغيرة (M-1):** ‏`store.js` بـ subscribe/notify (~30 سطراً بلا مكتبات) يملك `articles/customArticles/deletedIds` ويستهلكه `app.js` و`admin.js`.
4. **فصل الوصول إلى DOM:** `app.js` يخاطب `getElementById` مباشرة في 20+ موضعاً؛ كائن `elements` واحد يُهيأ عند الإقلاع يسهل الاختبار ويكشف الأخطاء مبكراً.
5. **الحفاظ على قرار «لا build step»:** لا نوصي بإدخال bundler أو framework — حجم المشروع وطبيعته لا يبرران ذلك، وهذه نقطة قوة لا ضعف.

---

## 8) فرص تبسيط الكود وتقليل التعقيد

1. توحيد `mergeArticles`/`rebuildMerged` (تكرار حرفي).
2. توحيد نمطي التهريب regex المتكررين في `improveLanguage` بدالة `escapeRegExp()` واحدة.
3. إزالة الكود الميت (L-1).
4. `renderCategoryFilter` يبني زر «الكل» بينما HTML يحتويه أصلاً — وحّد المصدر (اجعل JS يبني الكل دائماً).
5. سلسلة if/else في `suggestResponse` تختفي كلياً مع M-2 (جدول بيانات بدل تفريعات).
6. `computeStats`: المتغير `DAY` والحسابات واضحة — لا حاجة لتغيير؛ يُذكر إنصافاً أن الوحدة نظيفة.

---

## 9) فرص تحسين تجربة المطور (DX)

| الفرصة | التكلفة | الأثر |
| --- | --- | --- |
| `package.json` بسيط مع `npm run serve` (بديل عن تذكر أمر Python) و`npm test` | ساعة | يوحّد نقطة الدخول للمطورين |
| ESLint (flat config) + Prettier | ساعتان | يمنع فئة كاملة من الأخطاء (متغيرات ميتة، استيرادات غير مستخدمة — كان سيلتقط L-1 تلقائياً) |
| اختبارات وحدة بـ Vitest أو `node:test` للوحدات النقية (`analyzer`, `response`, `storage`, `data.mergeArticles`) | يوم | شبكة أمان لأهم منطق في التطبيق؛ هذه الوحدات نقية وقابلة للاختبار فوراً تقريباً |
| GitHub Actions: lint + test على كل PR، ونشر Pages من main | نصف يوم | يمنع كسر الإنتاج؛ حالياً أي push إلى main ينشر مباشرة بلا فحص |
| `.gitignore` + `LICENSE` (README يقول «متاح للاستخدام الحر» بلا ترخيص فعلي — الوضع القانوني الافتراضي: جميع الحقوق محفوظة!) | دقائق | يصحح تناقضاً قانونياً حقيقياً |
| `CONTRIBUTING.md` مختصر يشرح بنية ملفات JSON (schema المواد والنوايا) | ساعتان | يمكّن مساهمات المحتوى القانوني من غير المبرمجين |

---

## 10) فرص تقليل الديون التقنية

مرتبة بنسبة (الأثر ÷ التكلفة):

1. إصلاح H-1 + H-2 (أسطر معدودة، يغلقان أعلى خطرين).
2. حذف الكود الميت + توحيد الدمج (L-1, M-1-أ).
3. تحقق موحد للمواد (يغلق M-3 وM-5 معاً).
4. data-driven كامل للنوايا (M-2) — أكبر دين معماري حالي.
5. اختبارات + CI — الدين الأخطر على المدى الطويل: بدونها كل ما سبق قابل للانتكاس.

---

## 11) فرص زيادة المواد القانونية (المحتوى)

المحتوى الحالي: **30 مادة في 12 فئة، و8 قوالب، و9 أنماط نوايا** — بينما اللوائح التنفيذية لنظام المرافعات الشرعية تتجاوز 240 مادة. هذه أكبر فرصة لرفع القيمة الفعلية للأداة:

1. **أبواب غير مغطاة إطلاقاً وتَرِد كثيراً في استفسارات المستفيدين:**
   - **الاختصاص** (المكاني والنوعي والدولي) — استفسارات «أي محكمة أرفع فيها دعواي؟»
   - **رفع الدعوى وقيدها وصحيفتها** — «كيف أرفع دعوى؟»
   - **الإثبات** (الشهادة، اليمين، الإقرار، الخبرة، الكتابة، المعاينة) — باب ضخم غائب كلياً
   - **النقض والتماس إعادة النظر** — الاعتراض مغطى بالاستئناف فقط (مادة واحدة!)
   - **وقف الخصومة وانقطاعها وتركها**
   - **الطلبات العارضة والإدخال والتدخل**
   - **الأوامر على العرائض والحجز التحفظي**
   - **إجراءات المحاكمة عن بُعد** (الجلسات الإلكترونية عبر ناجز — الأكثر سؤالاً عملياً)
2. **تعميق الفئات الموجودة:** «الاعتراض» بمادة واحدة و«التنفيذ» بمادتين لا تكفي لصياغة ردود موثقة.
3. **قوالب إضافية مقترحة:** شطب الدعوى وطلب استمرار النظر بعد الشطب، طلب منع سفر، رد على طلب نقض، إحالة لمركز المصالحة، طلب تصحيح صك، الاعتراض على تقرير خبير.
4. **أنماط نوايا مقترحة:** «استفسار عن تنفيذ حكم»، «استفسار عن وكالة»، «استفسار عن اختصاص المحكمة»، «طلب جلسة عن بُعد»، «استفسار عن رسوم/تكاليف».
5. **حوكمة المحتوى:** أضف حقل `lastVerified` وإصدار للائحة في كل مادة، وschema JSON موثق — المحتوى القانوني يتقادم مع تعديلات الأنظمة، وردّ رسمي يستند إلى مادة معدلة أخطر من عدم الرد. **(هذه أهم توصية محتوى في التقرير.)**

---

## 12) إمكانية الوصول (Accessibility)

الوضع الحالي دون المستوى المطلوب لأداة عمل حكومية:

| الفجوة | الموقع | الإصلاح |
| --- | --- | --- |
| الـ modals بلا `role="dialog"` و`aria-modal` وبلا حبس تركيز (focus trap) وبلا إعادة تركيز عند الإغلاق | `index.html:162,218` | سمات ARIA + حلقة تركيز بسيطة، أو الانتقال إلى عنصر `<dialog>` الأصلي (يوفر معظم ذلك مجاناً) — **الموصى به** |
| بطاقات المواد والقوالب `div` قابلة للنقر بلا `tabindex` ولا `role="button"` ولا تفعيل بـ Enter | `ui.js:96,112` | `role="button" tabindex="0"` + معالجة keydown، أو استخدام `<button>` |
| الـ toast بلا `aria-live` — قارئ الشاشة لا يسمع «تم الحفظ» | `index.html:233` | `aria-live="polite"` |
| أزرار الأيقونات تعتمد `title` فقط | `index.html:40-42` | أضف `aria-label` |
| التبويبات بلا نمط ARIA tabs | `index.html:89-92` | `role="tablist/tab/tabpanel"` + `aria-selected` |

---

## 13) اقتراحات إعادة الهيكلة (Refactoring) — ملخص

1. `escapeHtml` آمن للخصائص (H-1) — **فوري**.
2. إحياء `setAdminState` في callback ‏`onChange` (H-2) — **فوري**.
3. `mergeArticles` مشتركة من `data.js` (M-1).
4. `validateArticle()` + `isSafeUrl()` موحدتان (M-3, M-5).
5. نوايا data-driven بمعرّفات slug ‏(M-2).
6. `store.js` مركزي — عند أول ميزة جديدة تمس الحالة، لا قبلها.
7. استبدال قوالب `innerHTML` ببناء DOM في `renderArticles`/`renderAdminList` — تدريجي، بعد تغطية الاختبارات.

---

## 14) ميزات جديدة مقترحة (مرتبة بالقيمة)

1. **تصدير/استيراد الأرشيف المحفوظ** (JSON/CSV) — الميزة موجودة للمواد وغائبة عن الأصل الأثمن: ردود الموظف؛ وتخفف أيضاً خطر M-4/M-6.
2. **PWA / عمل دون اتصال:** ‏manifest + service worker + خطوط ذاتية — أجهزة العمل الحكومية كثيراً ما تكون خلف قيود شبكة.
3. **متغيرات القوالب التفاعلية:** القوالب تحوي `[التاريخ]` و`[رقم الطلب]` كنص خام؛ استبدلها بحوار تعبئة سريع، واملأ ما استُخرج من الكيانات تلقائياً (رقم المذكرة مستخرج أصلاً في التحليل!).
4. **درجات تطابق مفهومة:** «تطابق 35» بلا سياق؛ حوّلها إلى نسبة أو تسميات (عالي/متوسط).
5. **نسخ الرد مع السند النظامي كاملاً** (نص المواد لا عناوينها فقط) كخيار ثانٍ للنسخ.
6. **بحث في الأرشيف بالفئة والتاريخ** إضافة للنص.
7. **إحصاءات أسبوعية بيانية بسيطة** (أعمدة CSS خالصة، دون مكتبات).

---

## 15) خطة التطوير التدريجية (الأثر الأعلى مقابل التكلفة الأدنى)

### المرحلة 1 — إصلاحات فورية (يوم واحد) 🔴
1. إصلاح `escapeHtml` (H-1) — 3 أسطر.
2. إصلاح تحديث قائمة الإدارة (H-2) — سطر واحد.
3. `isSafeUrl()` للتحقق من scheme الروابط (M-3).
4. try/catch حول كتابات localStorage مع toast واضح (M-4).
5. `confirm()` قبل الاستيراد المدمّر (M-5-أ).
6. حذف الكود الميت + إضافة LICENSE و`.gitignore` وfavicon.

### المرحلة 2 — الموثوقية والبيانات (أسبوع) 🟠
1. `validateArticle()` موحدة للنموذج والاستيراد مع تحقق نوعي.
2. نوايا data-driven كاملة (`boostCategories` + `responseText` + slugs) — M-2.
3. توحيد منطق الدمج — M-1.
4. تراجع بمستوى واحد عن استبدال الرد النهائي (M-7) + حد أقصى للأرشيف.
5. شاشة خطأ مع إعادة محاولة عند فشل تحميل البيانات (L-6).

### المرحلة 3 — شبكة الأمان (أسبوع) 🟡
1. `package.json` + ESLint + Prettier.
2. اختبارات وحدة للوحدات النقية (analyzer, response, storage, merge) — الهدف: تغطية المنطق اللغوي بحالات عربية حقيقية (تطبيع الهمزات، البادئات، الكيانات).
3. GitHub Actions: ‏lint + test على كل PR، ونشر Pages بعد نجاحهما.
4. CSP + استضافة الخطوط ذاتياً.

### المرحلة 4 — القيمة والمحتوى (مستمر) 🟢
1. توسيع المواد النظامية للأبواب غير المغطاة (القسم 11) مع حوكمة `lastVerified`.
2. إمكانية الوصول (القسم 12).
3. تصدير الأرشيف + متغيرات القوالب + PWA.

> **مبدأ حاكم للخطة:** لا تدخل المرحلة 4 (توسيع الميزات) قبل إنجاز المرحلة 3 (الاختبارات وCI) — كل توسيع بلا شبكة أمان يراكم ديناً جديداً فوق ما نُظّف.

---

## 16) أسئلة مفتوحة للمالك (تؤثر على بعض التوصيات)

1. هل تعمل الأداة على **أجهزة مشتركة** بين موظفين؟ (يحسم مستوى الاستجابة لـ M-6: من إخلاء مسؤولية إلى تشفير.)
2. هل ملفات استيراد المواد **تُتبادل فعلاً بين الموظفين** أم يستخدمها المطور فقط؟ (يحسم أولوية M-3/M-5 — افترضنا التبادل.)
3. هل هناك نية لاعتماد الأداة رسمياً داخل الوزارة؟ (يرفع أولوية إمكانية الوصول والخصوصية والـ CSP إلى إلزامية.)
4. من يتولى **التحقق الدوري من مطابقة نصوص المواد** لآخر تعديلات اللوائح؟ (يحسم تصميم حوكمة المحتوى في القسم 11.)

---

*أُعد هذا التقرير بعد قراءة كاملة للمستودع واختبار تشغيل فعلي في المتصفح؛ كل خطأ وُصف بأنه «مؤكد» جرى التحقق منه إما بإعادة إنتاجه في بيئة تشغيل حقيقية أو بتتبع مسار الكود بشكل قاطع. لم يُجرَ أي تعديل على كود التطبيق ضمن هذه المراجعة.*
