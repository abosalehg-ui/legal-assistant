# مِحَك — مراجعة مستودع «مساعد الردود للمستفيدين»

> **تاريخ المراجعة:** 15 أغسطس 2026
> **نوع المشروع المكتشف:** تطبيق ويب (Web App) — ليس لعبة
> **الرابرك المطبّق:** مراجعة التطبيقات (هندسة 45% + UX/UI 30% + أمن 25%)
> **المنهجية:** قراءة كاملة لكل ملفات المصدر (لا عيّنة)، تشغيل `npm test` و`npm run lint`، وتشغيل التطبيق فعلياً في Chromium مع قياسات DOM حقيقية للتحقق من كل ملاحظة قبل إدراجها.
> **الدرجة الإجمالية: 7.9 / 10**

> **ملاحظة على النطاق:** المستودع فيه `ENGINEERING_REVIEW.md` — مراجعة سابقة (يوليو 2026) نُفّذت بنودها بالكامل. هذي المراجعة **مستقلة وعلى الحالة الراهنة**، وما تكرر البنود المُغلقة إلا للإشارة إنها أُغلقت فعلاً بالتحقق. كل ملاحظة تحت هي **جديدة** أو **باقية بعد الإصلاح السابق**.

---

## 1. نظرة عامة سريعة

تطبيق ويب ثابت (static) يساعد موظفي المحاكم السعودية على تحليل رسائل المستفيدين وصياغة ردود رسمية مستندة إلى اللوائح التنفيذية لنظام المرافعات الشرعية. التدفق أربع خطوات: لصق الرسالة ← تحليل واستخراج المواد ← كتابة الرد بالعامية ← تحسين الصياغة ونسخ/حفظ/طباعة.

البنية: **Vanilla JS بـ ES Modules، بلا build step وبلا أي اعتمادية وقت تشغيل**. كل المنطق في المتصفح، والبيانات في ملفات JSON منفصلة، والتخزين في `localStorage`. يعمل كـ PWA عبر Service Worker.

| البند | التفصيل |
|---|---|
| التقنيات | HTML5 + CSS3 (متغيرات CSS) + JavaScript ES2023 Modules |
| اعتماديات وقت التشغيل | **صفر** — لا مكتبات إطلاقاً |
| أدوات التطوير | ESLint ^9، Prettier ^3، `node:test`، globals ^16 |
| PWA | `sw.js` + `manifest.webmanifest` + أيقونة SVG |
| الخطوط | Tajawal + Amiri مستضافة ذاتياً (13 ملف woff2) — بلا أي طلب لطرف ثالث |
| CI | GitHub Actions: تحقق JSON + lint + test على كل push/PR |
| حجم المستودع | ~30 ملف مصدري، **~5,290 سطر** (بدون `package-lock.json` والخطوط) |
| توزيع الأسطر | CSS 1,074 · JS 1,229 · JSON بيانات 1,276 · HTML 256 · اختبارات 405 · توثيق 728 |
| البيانات | 44 مادة نظامية · 13 قالب · 13 نمط نية · 41 مجموعة مرادفات · 344 مدخل عامية←فصحى |

**حالة البناء المُتحقق منها:** `npm test` → **29/29 ناجح**. `npm run lint` فشل في بيئة المراجعة فقط لأن `node_modules` غير مثبّت (`@eslint/js` مفقود) — الإعداد نفسه سليم و CI يشغّل `npm ci` قبله، فما هي ملاحظة على المشروع.

---

## 2. المراجعة التقنية والهندسية (45%)

### 2.1 البنية المعمارية وفصل المسؤوليات — **8 / 10**

الفصل ممتاز عموماً: تسع وحدات بمسؤولية واحدة واضحة لكل وحدة (`analyzer` تحليل نصي خالص، `response` توليد نص خالص، `storage` تخزين، `ui` عرض DOM فقط، `data` تحميل ودمج). الوحدات المنطقية **لا تلمس الـ DOM إطلاقاً**، وهذا سبب إمكانية اختبارها في Node بلا jsdom.

**الملاحظة الباقية — ازدواج ملكية الحالة بين `app.js` و`admin.js`:**

`js/admin.js` يحتفظ بنسخة كاملة من حالة البيانات، ويتم التزامن عبر رحلة دائرية:

```js
// admin.js — upsertCustom()
rebuildMerged();
state.onChange(state);   // ينادي app.js …

// app.js — initAdmin callback
app.data.articles = newState.articles;
setAdminState(newState);  // … الذي ينادي admin.js مرة ثانية بحالته هو نفسها
```

`setAdminState` يستقبل نفس الكائن الذي أرسله `admin.js` للتو، فيعيد إسناد حقوله على نفسها. النتيجة الصافية الوحيدة هي `renderAdminList()`. هذا يشتغل، لكنه يخفي حقيقة أن ما فيه مصدر واحد للحقيقة.

**الحل:** انقل حالة المواد إلى وحدة `store.js` صغيرة تصدّر `getArticles()` و`upsert()` و`remove()` و`subscribe(fn)`، وخلّ `app.js` و`admin.js` مشتركين (subscribers) بدل ما يتبادلون الحالة. يشيل حوالي 20 سطر تزامن ويلغي الدورة.

### 2.2 قابلية القراءة والصيانة — **8.5 / 10**

من الأفضل ما شفته في مشروع بهذا الحجم. الدوال قصيرة (أطولها `bindEvents` ~70 سطر وهي تسلسلية بلا تشعّب)، عمق التداخل لا يتجاوز 3، والتعليقات العربية تشرح **«ليش»** لا «وش»:

```js
// تعبئة رقم الطلب/الدعوى تلقائياً من آخر تحليل. التواريخ تُترك للموظف عمداً:
// تاريخ الرسالة الواردة ليس بالضرورة التاريخ المقصود في الرد الرسمي.
```

تعليق زي هذا يوفّر على المطور القادم نصف ساعة تفكير. وهذا النمط متكرر في `analyzer.js` و`storage.js` و`sw.js`.

**الخصم الوحيد:** `js/app.js` يستدعي `document.getElementById` **أكثر من 45 مرة** بسلاسل نصية مكررة (`'finalOutput'` وحده 7 مرات). أي إعادة تسمية في `index.html` تنكسر بصمت وقت التشغيل بلا أي تحذير من lint.

**الحل:** أضف في أعلى `app.js`:
```js
const el = new Proxy({}, { get: (_, id) => document.getElementById(id) });
// الاستخدام: el.finalOutput.value
```
أو أبسط — كائن `DOM` واحد يُبنى مرة في `start()`.

### 2.3 التكرار (DRY) و Code Smells — **7 / 10**

ثلاث حالات تكرار حقيقية موثّقة:

**(أ) منطق تصدير Blob مكرر حرفياً** — `js/app.js` في `exportArchive()` و`js/admin.js` في معالج `adminExport`:
```js
const blob = new Blob([JSON.stringify(X, null, 2)], { type: 'application/json' });
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url; a.download = `...-${new Date().toISOString().slice(0,10)}.json`;
a.click();
URL.revokeObjectURL(url);
```
سبعة أسطر متطابقة في ملفين. **الحل:** `downloadJson(data, prefix)` في `js/ui.js`.

**(ب) `safeSetItem` مكرر** — `js/data.js` فيه `safeSetItem()` و`js/storage.js` فيه `persist()`، ونفس الجسم `try { localStorage.setItem } catch { return false }`. **الحل:** وحدة `js/safe-storage.js` مشتركة.

**(ج) الأخطر — `setOutput()` يُتجاوز في ثلاثة مواضع.** الدالة أُنشئت خصيصاً لحفظ نسخة تراجع قبل استبدال الرد، لكن ثلاث عمليات تكتب على `output.value` مباشرة:

| الموضع | السطر | الأثر |
|---|---|---|
| `addSelectedArticleToOutput()` | `output.value = currentText` | لا تراجع بعد إضافة مادة |
| `clearOutput()` | `.value = ''` | **يمسح رد الموظف كاملاً بلا تراجع** |
| `clearInput()` | `.value = ''` | نفس الشيء |

**متحقق منه بالمتصفح:** بعد كتابة نص في «الرد النهائي» والضغط على «مسح الرد» → الحقل يفضى و`undoOutputBtn` **يبقى مخفياً** (`undoVisible: false`). موظف كتب رداً طويلاً وضغط الزر الخطأ يفقد شغله نهائياً، مع إن آلية التراجع موجودة أصلاً في الكود.

**الحل:** بدّل الأسطر الثلاثة لتستدعي `setOutput(...)` — إصلاح من ثلاث كلمات يغطي أخطر مسار فقدان بيانات في التطبيق.

### 2.4 معالجة الأخطاء والحالات الحدّية — **7.5 / 10**

الأساسيات مغطاة بجدية: كل كتابات `localStorage` محمية من `QuotaExceededError` وترجع `false` بدل ما ترمي، `loadSavedResponses` يتعافى من JSON تالف، فشل تحميل البيانات يعرض بطاقة خطأ مع زر «إعادة المحاولة»، والاستيراد يفرّق بين «ليس مصفوفة» و«امتلأ التخزين» و«ملف غير صالح».

**الثغرة المؤكدة — `sw.js` يرجع `undefined` عند عدم وجود نسخة مخزّنة:**
```js
event.respondWith(
    fetch(request)
        .then(...)
        .catch(() => caches.match(request, { ignoreSearch: true })),
);
```
`caches.match` يُرجع `undefined` إذا ما لقى تطابق. تمرير وعد يُحل إلى `undefined` لـ `respondWith()` يرمي `TypeError` في المتصفح، والطلب يفشل كخطأ شبكة خام بدل صفحة مفهومة. السيناريو: موظف أوفلاين يفتح مورداً ما زار صفحته قبل.

**الحل:**
```js
.catch(async () => (await caches.match(request, { ignoreSearch: true }))
    ?? new Response('غير متاح دون اتصال', {
        status: 503, headers: { 'Content-Type': 'text/plain; charset=utf-8' },
    }));
```

**ملاحظة ثانية:** استيراد الأرشيف يقرأ `await file.text()` ثم `JSON.parse` بلا أي حد لحجم الملف. ملف 50 ميجا يجمّد الواجهة تماماً. **الحل:** `if (file.size > 5 * 1024 * 1024) { showToast('الملف أكبر من 5 ميجا'); return; }`.

### 2.5 الأداء واستهلاك الموارد — **7 / 10**

**القياس الفعلي أولاً، لا التخمين:** شغّلت `improveLanguage` على نص ~2,500 حرف في Chromium → **21.2 مللي ثانية**. هذا **مقبول تماماً** ولا يُحس. فلا أدّعي إن فيه مشكلة أداء حالية.

لكن ثلاثة أنماط تتدهور خطياً مع نمو البيانات، وهي جديرة بالذكر لأن التطبيق يسمح باستيراد مواد بلا حد:

**(أ) `improveLanguage` يبني 359 كائن RegExp في كل نقرة** (344 عامية + 15 مصطلح)، وكلها ثوابت لا تتغير:
```js
sortedColloquial.forEach(([colloquial, formal]) => {
    const regex = new RegExp('(^|[^\\u0621-\\u064A])([وفبلك]?)' + escapeRegExp(colloquial) + '...', 'g');
    improved = improved.replace(regex, '$1$2' + formal);
});
```
مع 359 تمريرة كاملة على النص. **الحل:** ابنِ التعبيرات مرة واحدة عند تحميل البيانات (`let compiled = null;` مع كسل)، أو أفضل — تعبير واحد بتبادل `(a|b|c)` مع دالة استبدال. الحل الثاني يصلح كمان الخطر الكامن في **2.5-د** تحت.

**(ب) `findRelevantArticles` يعيد تطبيع نفس النصوص داخل حلقة متداخلة:**
```js
keywords.forEach(keyword => {
    article.keywords.forEach(k => { const normalizedK = normalizeArabic(k); ... });
    if (normalizeArabic(article.title).includes(normalizedKw)) score += 8;
    if (normalizeArabic(article.text).includes(normalizedKw)) score += 3;   // نص كامل، لكل كلمة مفتاحية
});
```
بـ 44 مادة و10 كلمات مفتاحية = **880 تطبيع لنصوص كاملة** لكل تحليل. تافه اليوم؛ يصير ملموساً عند 500 مادة. نفس النمط في `extractKeywords` (يطبّع كل مرادف في كل نداء) و`detectIntent`.

**الحل:** طبّع مرة واحدة في `loadData()` واحفظ `_normTitle` و`_normText` و`_normKeywords` مع كل مادة. تحويل من O(مواد × كلمات) إلى O(مواد) لمرة واحدة عند التحميل.

**(ج) Service Worker شبكة-أولاً لكل شيء** — كل زيارة تنتظر الشبكة قبل العرض حتى لو النسخة المخزّنة موجودة وصالحة. على شبكة محكمة بطيئة أو متذبذبة، هذا يعني تطبيق بطيء الإقلاع دائماً رغم إنه PWA.

**الحل:** أصول ثابتة (CSS/JS/خطوط) → **cache-first**؛ ملفات `data/*.json` و`index.html` → **stale-while-revalidate** (اعرض المخزّن فوراً وحدّث بالخلفية). التطبيق يصير فورياً مع بقاء التحديث التلقائي.

**(د) خطر كامن في `improveLanguage`** — 344 استبدالاً متتابعاً، ومخرج استبدال مبكر **قابل لأن يطابقه مفتاح لاحق**. فحصت القاموس الحالي: ما فيه سلسلة ضارة اليوم (الحالات الـ14 المتقاطعة كلها نقاط ثابتة مثل `أعطي → أعطي`). لكنه خطر يظهر أول ما يضيف أحدهم مدخلاً. الحل نفسه في (أ) — تمريرة واحدة بتبادل — يلغيه بنيوياً.

**(هـ) 11 مدخلاً ميتاً في `data/colloquial-map.json`** — مفاتيح تُستبدل بنفسها حرفياً: `سمعت`، `سمعنا`، `يسمع`، `يعطي`، `تعطي`، `أعطي`، `آخذ`، `يقول`، `تقول`، `أقول`، `جلستي`. كل واحد منها يبني RegExp ويمرّ على النص كامل بلا أي أثر.

### 2.6 الاختبارات — **7 / 10**

**29 اختباراً، كلها ناجحة**، وهي أفضل جودةً من مجرد عدد: تغطي حدود الكلمة العربية، تعافي JSON التالف، سلوك امتلاء التخزين (بـ stub لـ `localStorage`)، ورفض الروابط غير الآمنة. أسماء الاختبارات وصفية بالعربي. هذا أعلى بكثير من متوسط المشاريع المشابهة.

**الفجوة الحقيقية — التغطية موزّعة بشكل غير متوازن:**

| الوحدة | مغطاة؟ |
|---|---|
| `analyzer.js`, `response.js`, `storage.js`, `data.js` | ✅ جيد |
| `ui.js`, `admin.js`, `app.js`, `shortcuts.js`, `theme.js` | ❌ **صفر** |

الملاحظة المهمة: **كل الأخطاء الوظيفية اللي أثبتّها في هذي المراجعة (2.3-ج، 3.2، 3.5) تقع في الوحدات غير المغطاة.** المنطق النقي — وهو الجزء الأسهل والأقل عرضة للانكسار — مختبَر جيداً؛ وطبقة الـ DOM حيث الأخطاء فعلاً موجودة، غير مختبرة إطلاقاً.

**الحل:** أضف `jsdom` كاعتمادية تطوير واختبر `renderArticles` (تهريب HTML + حالة `aria-pressed`) و`escapeHtml` و`openModal/closeModal` (حبس التركيز واستعادته). خمسة اختبارات DOM كانت كفيلة بالتقاط ثلاث ملاحظات من هذا التقرير.

### 2.7 التوثيق — **9 / 10**

`README.md` توثيق نموذجي: نظرة عامة، جدول ميزات، مخطط تدفق، تشغيل محلي **مع تفسير سبب فشل `file://`** (نقطة يقع فيها معظم الناس)، هيكل مشروع كامل، جدول اختصارات. و`CONTRIBUTING.md` يشرح بنية ملفات JSON — وهو بالضبط ما يحتاجه من يبي يضيف مادة نظامية. ووجود `ENGINEERING_REVIEW.md` بمنهجية موثّقة نضج نادر.

**الخصم:** التعليق أعلى `sw.js` يقول *«الاستراتيجية: الشبكة أولاً … يضمن أحدث نسخة عند توفر الاتصال ويبقي التطبيق عاملاً بدونه»* — صحيح وصفياً لكنه ما يذكر المقايضة (بطء الإقلاع الدائم، انظر 2.5-ج). و`README` يصف التحليل بـ«ذكي» و«تلقائي» بينما هو **مطابقة كلمات مفتاحية بأوزان** — دقيق وشفاف داخل الكود، ومبالغ فيه قليلاً في التسويق.

### 2.8 اتفاقيات التسمية ونمط موحّد — **9 / 10**

`camelCase` بلا استثناء واحد عبر 1,229 سطر JS. مسافة بادئة 4 موحّدة. `.prettierrc.json` و`eslint.config.js` بإعدادات منفصلة صحيحة لكل بيئة (browser / serviceworker / node). أسماء الدوال أفعال، أسماء البيانات أسماء. مفاتيح JSON `camelCase` إنجليزي والقيم عربية — فصل صحيح.

**الخصم البسيط:** خلط طفيف بين الفئات المنطقية والفئات الشكلية في CSS (`.full-width` و`.hidden` و`.mt-1` نفعية، والباقي دلالي) — مقبول لكن غير موثّق.

---

### 📊 متوسط القسم الهندسي

| النقطة | الدرجة |
|---|---|
| البنية المعمارية | 8.0 |
| قابلية القراءة والصيانة | 8.5 |
| التكرار و Code Smells | 7.0 |
| معالجة الأخطاء | 7.5 |
| الأداء | 7.0 |
| الاختبارات | 7.0 |
| التوثيق | 9.0 |
| التسمية والنمط | 9.0 |

**متوسط القسم = 63 ÷ 8 = 7.9 / 10**

---

## 3. المظهر والتصميم وتجربة المستخدم (30%)

### 3.1 الاتساق البصري — **9 / 10**

نظام تصميم حقيقي: `css/themes.css` يعرّف **28 متغيراً** يغطي الألوان والظلال ونصف الأقطار وحلقة التركيز والتدرجات، وكل الوضع الداكن مجرد إعادة تعريف لنفس المتغيرات. لوحة أخضر/ذهبي مناسبة للسياق القضائي السعودي. خطان بدورين واضحين: Tajawal للواجهة وAmiri للنص القانوني في مربع الإخراج والطباعة — اختيار مقصود وصحيح.

**الخصم:** ألوان صريحة تفلت من النظام في `css/styles.css`:
```css
.tag.urgent { background: #e67e22; }
.tag.tone-complaint { background: #c0392b; }
.tag.tone-inquiry  { background: #2980b9; }
.tag.tone-request  { background: #8e44ad; }
.tag.tone-neutral  { background: #7f8c8d; }
.btn-danger { background: #c0392b; }
```
ستة ألوان ثابتة **لا تتبدّل في الوضع الداكن**، فتظهر بنفس تشبّعها العالي على خلفية داكنة. و`css/print.css` يكرر `#1a5f4a` صراحةً بدل المتغير.

**الحل:** أضف `--tone-complaint` و`--tone-inquiry` … إلخ إلى `themes.css` مع قيم داكنة أهدأ.

### 3.2 سهولة الاستخدام والتنقل — **7.5 / 10**

التدفق ممتاز: أربع بطاقات مرقّمة منطقياً، والمهمة الأساسية (رسالة ← رد جاهز للنسخ) تتم في **نقرتين** — «تحليل الرسالة» ثم «نسخ الرد». صعب تحسينه.

**🔴 خطأ وظيفي مؤكد بالمتصفح — نتائج التحليل تضيع عند مسح خانة البحث:**

```js
function searchArticles() {
    const query = document.getElementById('articleSearch').value.trim();
    if (!query) {
        filterByCategory(app.currentFilter);   // ← يعيد بناء القائمة من كل المواد
        return;
    }
    ...
}
```

المشكلة أن `analyzeMessage()` تكتب نتائج مرتّبة بالصلة عبر `renderArticles()` مباشرة، **دون تسجيل هذي الحالة في أي مكان**. فأول ما المستخدم يبحث ثم يمسح البحث، `filterByCategory` يستبدل النتائج بكل المواد الـ44 بترتيب الملف.

**القياس الفعلي في Chromium:**

| الحالة | عدد المواد المعروضة | أول مادة | شارات «تطابق» |
|---|---|---|---|
| بعد التحليل | **10** | **المادة 40** | **10** |
| بعد الكتابة في البحث ثم مسحه | **44** | المادة 1/1 | **0** |

كل عمل التحليل يضيع بلا أي إشعار، وترتيب الصلة يُستبدل بترتيب عشوائي فعلياً. المسار طبيعي جداً: يحلل، يبحث عن مادة معينة، يمسح البحث ليرجع للنتائج — فيجد نفسه أمام قائمة كاملة غير مرتّبة.

**الحل في `js/app.js`:** خزّن آخر نتيجة تحليل `app.lastRelevantArticles = relevantArticles;` داخل `analyzeMessage()`، وفي `searchArticles()` عند فراغ الاستعلام:
```js
if (!query) {
    if (app.currentFilter === 'all' && app.lastRelevantArticles) {
        renderArticles(app.lastRelevantArticles, app.selectedArticleIds);
    } else {
        filterByCategory(app.currentFilter);
    }
    return;
}
```

**ملاحظتان أخف:**
- **حوارات النظام الأصلية:** التطبيق فيه نظام Modal أنيق (حبس تركيز، تراكب مموّه، تنسيق كامل)، ومع ذلك كل تأكيدات الحذف تستخدم `confirm()` الأصلي في `app.js` و`admin.js` (4 مواضع) — نافذة رمادية بخط النظام تكسر الانطباع، وفي بعض المتصفحات تظهر بلا اتجاه RTL.
- **«مسح الرد» بلا شبكة أمان** (انظر 2.3-ج).

### 3.3 الاستجابة — **8 / 10**

**متحقق منه فعلياً على 390×844 (حجم iPhone 14):** `bodyScrollWidth = 390` و`innerWidth = 390` → **لا يوجد أي فيض أفقي**. الشبكة تنهار من عمودين إلى عمود واحد عند 1024px، والرأس يتكدّس عمودياً عند 768px، والأزرار تصير بعرض كامل. اللقطة على الجوال نظيفة ومقروءة.

**الخصم:** أهداف اللمس تحت الحد الموصى به (تفصيلها في 3.5).

### 3.4 العربية و RTL — **6.5 / 10**

الأساس سليم: `<html lang="ar" dir="rtl">`، والبيان `"dir": "rtl", "lang": "ar"`، وخطوط عربية حقيقية مستضافة ذاتياً مع `unicode-range` صحيح و`font-display: swap` (النص يظهر فوراً بخط بديل بدل صفحة فاضية). ومعظم الحدود اتجاهية صحيحة (`border-right` على بطاقات المواد = الحافة البادئة في RTL).

**🔴 خطأ RTL مؤكد بصرياً وبالقياس — أيقونة البحث تحجب أول حرف في خانتَي البحث:**

```css
.search-box input { padding: 0.75rem 1rem 0.75rem 2.5rem; }  /* أعلى | يمين | أسفل | يسار */
.search-box .search-icon { position: absolute; right: 1rem; }
```

الأيقونة موضوعة على اليمين (صحيح في RTL)، لكن المساحة الإضافية (`2.5rem`) محجوزة على **اليسار** — الجهة الخاطئة تماماً. النص العربي يبدأ من اليمين على بعد `1rem` فقط، أي **تحت الأيقونة مباشرة**.

**القياس في Chromium:**

| الخانة | `padding-right` | `padding-left` | بداية النص | حافة الأيقونة | التداخل |
|---|---|---|---|---|---|
| `#articleSearch` | 16px | 40px | x=644 | x=624 | **20px** |
| `#savedSearch` | 16px | 40px | x=1328 | x=1308 | **20px** |

اللقطة تؤكده: عند كتابة «تبليغ الخصم بموعد الجلسة المحددة» يختفي حرف **«ت»** خلف عدسة البحث. يصيب **كلا** خانتَي البحث — أي كل بحث في التطبيق.

**الحل في `css/styles.css`:**
```css
.search-box input { padding-block: 0.75rem; padding-inline: 2.5rem 1rem; }
.search-box .search-icon { inset-inline-start: 1rem; }
```
الخصائص المنطقية (`padding-inline`, `inset-inline-start`) تصلح العطل وتحمي من تكراره لو أُضيفت واجهة LTR لاحقاً.

**🟠 تضارب في شكل الأرقام والتقويم داخل الشاشة الواحدة:**

| الموضع | المخرج الفعلي المُقاس |
|---|---|
| تاريخ الرد المحفوظ (`storage.js`) | `٢‏/٣‏/١٤٤٨ هـ` — أرقام هندية + هجري |
| عدّادات الرأس والإحصائيات | `44` · `0` — أرقام عربية غربية |
| اسم ملف الأرشيف المُصدَّر (`app.js`) | `responses-2026-08-15.json` — ميلادي ISO |

ثلاثة أنظمة ترقيم/تقويم معروضة معاً. الهجري بالأرقام الهندية **صحيح ومناسب** لأداة قضائية سعودية — الملاحظة ليست عليه، بل على أن بقية الأرقام ما تتبعه. والأهم عملياً: **اسم الملف المُصدَّر ميلادي**، فموظف يصدّر أرشيفه في `1448/3/2` يحصل على ملف اسمه `2026-08-15` ويصعب عليه ربطه بسجلاته.

**الحل:** وحّد عبر دالة `formatDate()` واحدة في وحدة مشتركة تستخدم `ar-SA-u-ca-islamic-umalqura`، وطبّق `Intl.NumberFormat('ar-SA')` على العدّادات، وأضف التاريخ الهجري لاسم الملف المصدَّر إلى جانب الميلادي.

**نقطة تستحق الذكر:** `normalizeArabic()` معالجة عربية صحيحة فعلاً — توحيد الهمزات، الألف المقصورة، التاء المربوطة، إزالة التشكيل والتطويل — ومختبَرة بثمانية تأكيدات. أكثر من مطوّر بيبني بحثاً عربياً بـ `toLowerCase()` وخلاص.

### 3.5 إمكانية الوصول — **6.5 / 10**

فيه استثمار حقيقي هنا، لا مجرد `aria-label` مرشوشة: حبس تركيز كامل داخل النوافذ مع **استعادة التركيز للعنصر السابق** عند الإغلاق، `role="status" aria-live="polite"` على التوست، عناصر المواد `role="button" tabindex="0"` مع تفعيل بـ Enter/Space عبر تفويض واحد، `aria-selected` على التبويبات، و`aria-label` على كل أزرار الأيقونات.

ومع ذلك، أربع فجوات مؤكدة بالقياس:

**(أ) ثلاثة عناصر إدخال بلا اسم مُتاح إطلاقاً** — لا `<label>` ولا `aria-label`، فقط `placeholder`:

| العنصر | الدور |
|---|---|
| `#articleSearch` | البحث في المواد النظامية |
| `#savedSearch` | البحث في الأرشيف |
| `#finalOutput` | **مربع الرد النهائي — العنصر المحوري في التطبيق** |

قارئ الشاشة ينطق «مربع تحرير» بلا أي سياق. و`placeholder` **يختفي** أول ما يُكتب فيه، فالمستخدم يفقد حتى هذا التلميح.

**الحل:** `aria-label="البحث في المواد النظامية"` للخانتين، و`<label for="finalOutput">` مرئي فوق مربع الإخراج (فيه أصلاً عنوان بطاقة «الرد النهائي» — اربطه بـ `aria-labelledby`).

**(ب) `aria-pressed` يبقى `"true"` بعد إلغاء التحديد** — متحقق منه:
```js
// clearInput() تزيل الفئة البصرية فقط
document.querySelectorAll('.article-item').forEach(el => el.classList.remove('selected'));
```
بعد الضغط على «مسح»: `hasSelectedClass: false` لكن `ariaPressed: "true"`. المستخدم المبصر يرى المادة غير محددة، وقارئ الشاشة يقول «مضغوط». **الحل:** `el.setAttribute('aria-pressed', 'false')` في نفس الحلقة.

**(ج) تباين نصي دون معيار WCAG AA** — قياس في الوضع الفاتح:
```
.saved-item-date → #8a8a8a على #f8f9fa = نسبة 3.28 : 1 (الحجم 12px)
المطلوب: 4.5 : 1 للنص العادي
```
`--text-light` مستخدم في `.saved-item-date` و`.empty-state` و`.admin-list-item .item-meta` و`.output-box::placeholder`. **الوضع الداكن يمر** (~4.6:1) — العطل في الفاتح فقط. **الحل:** غيّر `--text-light` من `#8a8a8a` إلى **`#6b6b6b`** (≈ 5.3:1) في `:root` فقط.

**(د) أهداف لمس أصغر من الحد** — قياسات فعلية:

| العنصر | المقاس | WCAG 2.5.8 (24px) | الموصى به للجوال (44px) |
|---|---|---|---|
| أزرار تحميل/حذف الرد المحفوظ | **20 × 19 px** | ❌ **فشل** | ❌ |
| أزرار تعديل/حذف في شاشة الإدارة | 32 × 32 px | ✅ | ❌ |
| أزرار الرأس (سمة/إدارة/اختصارات) | 40 × 40 px | ✅ | ❌ |
| أزرار الفئات | 51 × 31 px | ✅ | ❌ |

زر **حذف** بمقاس 20×19px بجانب زر **تحميل** بنفس المقاس، بفاصل 4px — هذا خطأ نقر مؤكد على الجوال، على عملية مدمّرة. **الحل:** `min-width: 44px; min-height: 44px;` على `.saved-item-actions button` مع `padding: 0.6rem`.

> **تصحيح لقياس أوّلي:** ظهر `.modal-close` بمقاس `0×0` في القياس الآلي — وهذا لأن النافذة كانت مغلقة (`display:none`) وقت القياس، لا لأنه عطل. مقاسه الفعلي 32×32px. ما أدرجته كملاحظة.

### 3.6 التغذية الراجعة — **8 / 10**

توست عربي واضح على **كل** إجراء، بما فيها حالات الفشل («تعذّر الحفظ: امتلأت مساحة التخزين، احذف بعض الردود القديمة» — رسالة تقول للمستخدم وش يسوي، لا مجرد «خطأ»). بطاقة فشل التحميل فيها زر إعادة محاولة يعمل. تأكيد قبل كل حذف. تعليقات الاستيراد تفصّل: «تم استيراد 12 مادة (رُفضت 3 لعدم اكتمال بياناتها)» — شفافية ممتازة.

**الخصم:** ما فيه حالة تحميل (loading) أثناء `loadData()` — الصفحة تظهر بعدّادات صفرية وشبكة فارغة حتى تصل ملفات JSON. على اتصال بطيء تبدو معطّلة. **الحل:** هيكل عظمي (skeleton) بسيط أو `aria-busy="true"` على الحاوية حتى انتهاء `start()`.

---

### 📊 متوسط قسم UX/UI

| النقطة | الدرجة |
|---|---|
| الاتساق البصري | 9.0 |
| سهولة الاستخدام والتنقل | 7.5 |
| الاستجابة | 8.0 |
| العربية و RTL | 6.5 |
| إمكانية الوصول | 6.5 |
| التغذية الراجعة | 8.0 |

**متوسط القسم = 45.5 ÷ 6 = 7.6 / 10**

---

## 4. المراجعة الأمنية (25%)

**تحديد السياق أولاً:** تطبيق ثابت أوفلاين بالكامل، **بلا خادم خلفي، بلا قاعدة بيانات، بلا مصادقة، بلا أي طلب شبكة لطرف خارجي**. لذلك بنود **الصلاحيات والمصادقة** و**CORS** و**حقن SQL** **لا تنطبق** — ولن أخترع لها ثغرات. الدرجة محسوبة من البنود المنطبقة فقط.

### 🔑 المفاتيح والأسرار — **10 / 10** — ✅ نظيف

فحصت الملفات المتتبَّعة **وتاريخ git كاملاً** (`git log --all -p`) بحثاً عن `api_key`/`secret`/`password`/`token`/مفاتيح خاصة → **لا شيء**. لا `.env` ولا بيانات اعتماد. المشروع بطبيعته لا يحتاج أي سر.

### 💉 الحقن — **8.5 / 10**

**الثغرة الحرجة السابقة مُغلقة ومُتحقق منها:** `escapeHtml` في `js/ui.js` الآن يهرّب الخمسة أحرف كاملة بما فيها `"` و`'`:
```js
.replace(/"/g, '&quot;').replace(/'/g, '&#39;');
```
فمسار XSS في سياق الخصائص عبر ملف مواد مستورد **مسدود**. وكذلك `isSafeUrl()` يقبل `https:` فقط فيمنع `javascript:`، و`CSS.escape()` مستخدم في محدد `data-id`، و`rel="noopener"` على الروابط الخارجية.

**🟡 ملاحظة باقية — قيمتان تُحقنان في الـ HTML بلا تهريب:**
```js
// js/ui.js — renderTemplates()
`<div class="template-card" data-id="${template.id}" ...>`
// js/ui.js — renderSavedResponses()
`<button data-action="load" data-id="${response.id}" ...>`
```
تتبّعت المصدرين، وكلاهما **محمي فعلياً اليوم**: `template.id` يأتي من `data/templates.json` المملوك للتطبيق، و`response.id` مضمون أنه رقم لأن `importResponses()` يفرض `typeof raw.id === 'number'` وإلا يستبدله. فما هي ثغرة قابلة للاستغلال — لكنها **اعتماد ضمني غير موثّق**: أي تخفيف مستقبلي لتحقق `id` يفتح الثغرة بلا إنذار.

**الحل:** لفّهما بـ `escapeHtml()` للاتساق (تكلفة صفرية)، أو أضف تعليقاً يوثّق الاعتماد.

**🟡 نمط بنيوي:** كل العرض يمر عبر `innerHTML` مع قوالب نصية. سليم اليوم لأن التهريب منضبط، لكن أمانه يعتمد على انضباط بشري في كل سطر جديد. لو نمت الواجهة، `textContent` مع بناء عناصر (أو `<template>`) يخلي الأمان بنيوياً بدل انضباطي.

### ✅ التحقق من المدخلات — **8 / 10**

`validateArticle()` في `js/data.js` تحقق جاد: يرفض غير الكائنات، يفرض وجود الحقول الإلزامية بعد `trim`، ينظف `keywords` غير المصفوفية، **ويجرّد `sourceUrl` غير الآمن بدل رفض المادة كلها**. و`importResponses()` يتحقق من كل عنصر ويحد الأرشيف بـ `MAX_SAVED = 500`.

**🟡 ما ينقص:** لا حد لحجم الملف المستورد (انظر 2.4)، ولا حد لطول نص المادة أو الرد. ملف مصنوع فيه نص بملايين الأحرف يمكن أن يملأ حصة `localStorage` أو يجمّد الواجهة. الأثر: تعطيل ذاتي محلي فقط (المستخدم نفسه هو من يستورد)، لذلك 🟡 لا أكثر.

### 🔒 البيانات الحساسة — **6 / 10** 🟠 — **أهم ملاحظة أمنية في التقرير**

هذا هو الجانب الذي يستحق أكثر من غيره، بسبب **طبيعة البيانات لا بسبب عيب برمجي**.

التطبيق **يستخرج عمداً** بيانات شخصية ويعرضها ويخزّنها:
```js
// js/analyzer.js — extractEntities()
(originalText.match(/\b05\d{8}\b/g)  || []).forEach(p  => add('رقم جوال', p));
(originalText.match(/\b[12]\d{9}\b/g) || []).forEach(id => add('رقم هوية', id));
```

أرقام هوية وطنية وأرقام جوال وتفاصيل قضايا مستفيدين، تُحفظ **بنص صريح** في `localStorage` على أجهزة عمل في محاكم — أجهزة كثيراً ما تكون مشتركة بين موظفي المناوبة. وواقع `localStorage`:

- **بلا انتهاء صلاحية** — يبقى للأبد حتى يُمسح يدوياً
- **بلا مسح شامل** — لا يوجد زر «امسح كل الأرشيف»؛ الحذف فردي رد-برد فقط، و**500 رد** حد أقصى
- **يقرأه أي سكربت** على نفس الأصل
- **يبقى بعد تسجيل الخروج** من حساب ويندوز، ويُنسخ ضمن ملفات المتصفح في النسخ الاحتياطي للجهاز
- **يُصدَّر بضغطة واحدة** إلى ملف JSON عادي بلا حماية

**التقييم العادل:** التشفير الكامل **مبالغة** لأداة أوفلاين — المفتاح لازم يُخزّن بنفس المكان، فما يضيف شيئاً حقيقياً أمام من يملك الجهاز. لكن الغياب التام لأي سياسة احتفاظ ببيانات شخصية لمستفيدين ملاحظة صحيحة ومطلوبة.

**الحل (متناسب وقابل للتنفيذ):**
1. زر **«مسح كل الأرشيف»** في بطاقة الردود المحفوظة مع تأكيد — أبسط تخفيف وأعلاه أثراً
2. **حذف تلقائي بعد N يوماً** (افتراضي 30) — `timestamp` موجود أصلاً في كل عنصر، فالتنفيذ مرشّح واحد في `loadSavedResponses()`
3. **تنبيه صريح في الواجهة**: «تُحفظ الردود على هذا الجهاز فقط — لا تستخدم التطبيق على جهاز عام»
4. **إخفاء جزئي في المعاينة**: اعرض `١٠٩٢***٤٥٦٧` بدل رقم الهوية كاملاً في قائمة الأرشيف

### 📦 الاعتماديات — **10 / 10** — ✅ ممتاز

**صفر اعتماديات وقت تشغيل** (`"dependencies"` غير موجود إطلاقاً). أربع اعتماديات تطوير فقط، كلها أدوات معروفة بإصدارات حديثة (ESLint 9، Prettier 3، globals 16). **سطح هجوم سلسلة التوريد معدوم تقريباً** — وهذا في 2026 ميزة معمارية كبيرة، لا مجرد بساطة. حتى الخطوط مستضافة ذاتياً، فلا يوجد أي طلب لـ Google Fonts ولا أي تتبّع لطرف ثالث.

### 🛡️ الترويسات و CSP — **7 / 10**

سياسة أمن محتوى صريحة في `index.html`:
```
default-src 'self'; script-src 'self'; style-src 'self'; font-src 'self';
img-src 'self' data:; connect-src 'self'; manifest-src 'self';
object-src 'none'; base-uri 'self'; form-action 'self'
```

**بلا `unsafe-inline` وبلا `unsafe-eval`** — وهذا نادر فعلاً. و**تحققت من أنها تُطبَّق حقيقةً**: محاولة حقن نمط مضمّن أثناء المراجعة رُفضت من المتصفح:
> `Refused to apply inline style because it violates the following Content Security Policy directive: "style-src 'self'"`

سياسة عاملة، لا زخرفة. وهي تحوّل بند «الحقن» أعلاه من خطر إلى خطر مُخفَّف بطبقة ثانية.

**🟡 الفجوة:** `frame-ancestors` و`X-Frame-Options` **لا يمكن ضبطهما عبر `<meta>`** — يتجاهلهما المتصفح ويتطلبان ترويسة HTTP، وGitHub Pages ما يسمح بترويسات مخصصة. فالتطبيق قابل للتضمين في `<iframe>` من أي موقع (خطر clickjacking). **الأثر منخفض حقيقةً**: ما فيه مصادقة ولا إجراء ذو امتياز يُخدع المستخدم لتنفيذه.

**الحل:** إن انتقل النشر لاحقاً إلى استضافة تدعم الترويسات (Cloudflare Pages / Netlify / Nginx)، أضف `Content-Security-Policy: frame-ancestors 'none'` و`X-Content-Type-Options: nosniff` و`Referrer-Policy: no-referrer`. على GitHub Pages، حارس سطر واحد يكفي: `if (window.top !== window.self) window.top.location = window.self.location;`

---

### 📊 متوسط القسم الأمني

| النقطة | الدرجة |
|---|---|
| المفاتيح والأسرار | 10.0 |
| الحقن | 8.5 |
| التحقق من المدخلات | 8.0 |
| الصلاحيات والمصادقة | **لا ينطبق** — بلا خادم |
| البيانات الحساسة | 6.0 |
| الاعتماديات | 10.0 |
| الترويسات و CSP | 7.0 |

**متوسط القسم = 49.5 ÷ 6 بنود منطبقة = 8.3 / 10**

---

## 5. الدرجة الإجمالية المرجّحة

| الناحية | الدرجة | الوزن | المساهمة |
|---|---|---|---|
| تقنية وهندسية | 7.9 / 10 | 45% | 3.555 |
| UX / UI | 7.6 / 10 | 30% | 2.280 |
| أمن سيبراني | 8.3 / 10 | 25% | 2.075 |
| **الإجمالي** | | **100%** | **7.91 / 10** |

**الحساب صريحاً:**
```
(7.9 × 0.45) + (7.6 × 0.30) + (8.3 × 0.25)
=  3.555     +  2.280       +  2.075
=  7.91  →  7.9 / 10
```

### وش تعنيه 7.9

مشروع **فوق المتوسط بوضوح** ويستحق التقدير: اختيار معماري صائب (بلا اعتماديات، بلا build)، اختبارات حقيقية تنجح، CI عامل، CSP مُطبَّقة فعلاً، فصل بيانات عن كود، ووعي أمني حقيقي — لا شكلي.

ما يمنعه من 9 ليس ضعفاً معمارياً، بل **ثلاثة عيوب مرئية في مسارات يمر عليها الموظف يومياً**: أيقونة تحجب أول حرف في كل بحث، نتائج تحليل تختفي بلا سبب ظاهر، وزر مسح يمحو الشغل بلا تراجع مع أن آلية التراجع مكتوبة أصلاً. الثلاثة **إصلاحات صغيرة** — لا إعادة بناء. وبينها وبين تصحيح التباين وأهداف اللمس، الوصول لـ **8.7+** شغل يوم واحد.

**ما لا يمكن تقييمه من الكود وحده:**
- **دقة المحتوى النظامي** — الحكم على صحة نصوص المواد الـ44 ومطابقتها لأحدث اللوائح يحتاج مراجعة قانونية مختصة، لا مراجعة برمجية. كل المواد تحمل `lastVerified: "2026-07-16"` وروابط رسمية لـ `laws.boe.gov.sa` (44/44 — لا مادة بلا مصدر)، وهذا انضباط ممتاز — لكن التحقق نفسه خارج نطاقي.
- **جودة الردود المولّدة عملياً** — هل الرد المقترح مقبول لموظف محكمة فعلي؟ يحتاج اختبار ميداني.
- **دقة المطابقة** — هل النية والمواد المستخرجة صحيحة على رسائل حقيقية متنوعة؟ يحتاج مجموعة تقييم مُصنَّفة يدوياً.

---

## 6. خطة الإصلاح المرتبة بالأولوية

مرتّبة بـ **(الأثر ÷ الجهد)**، لا بترتيب الأقسام. البنود 1–5 مجتمعةً أقل من يوم عمل.

| # | الأولوية | الناحية | المشكلة | الحل المقترح | التأثير المتوقع |
|---|---|---|---|---|---|
| 1 | 🔴 حرج | UX / RTL | أيقونة البحث تحجب أول حرف في `#articleSearch` و`#savedSearch` (تداخل 20px مؤكد بصرياً وبالقياس) | `css/styles.css` → `.search-box input { padding-block:.75rem; padding-inline:2.5rem 1rem; }` و`.search-icon { inset-inline-start:1rem; }` | أوضح عطل بصري في التطبيق يختفي؛ البحث يصير مقروءاً |
| 2 | 🔴 حرج | UX / منطق | نتائج التحليل (10 مواد مرتّبة بالصلة) تُستبدل بكل المواد الـ44 عند مسح خانة البحث | `js/app.js` → خزّن `app.lastRelevantArticles` في `analyzeMessage()`، وأعد عرضها في `searchArticles()` عند فراغ الاستعلام والفلتر `all` | يوقف ضياع عمل التحليل صامتاً في مسار استخدام يومي |
| 3 | 🔴 حرج | فقدان بيانات | «مسح الرد» و«مسح» وإضافة مادة تتجاوز `setOutput()` فلا تسجّل نسخة تراجع | `js/app.js` → بدّل `output.value = X` إلى `setOutput(X)` في `clearOutput()` و`clearInput()` و`addSelectedArticleToOutput()` | ثلاث كلمات تحمي رداً كتبه الموظف من الضياع نهائياً |
| 4 | 🟠 مهم | إتاحة وصول | `#finalOutput` و`#articleSearch` و`#savedSearch` بلا اسم مُتاح لقارئ الشاشة | `index.html` → `aria-label` للخانتين، و`aria-labelledby` يربط `#finalOutput` بعنوان بطاقته | العنصر المحوري في التطبيق يصير مفهوماً لقارئ الشاشة |
| 5 | 🟠 مهم | إتاحة وصول | زرّا «تحميل/حذف» الرد المحفوظ بمقاس 20×19px بفاصل 4px — دون حد WCAG 2.5.8 (24px) | `css/styles.css` → `.saved-item-actions button { min-width:44px; min-height:44px; padding:.6rem; }` | يمنع حذفاً بالخطأ على الجوال؛ يحقق معيار حجم الهدف |
| 6 | 🟠 مهم | إتاحة وصول | تباين `--text-light` = 3.28:1 في الوضع الفاتح (المطلوب 4.5:1) | `css/themes.css` → `--text-light: #6b6b6b` في `:root` فقط (الوضع الداكن سليم) | التواريخ والبيانات الثانوية تصير مقروءة؛ توافق AA |
| 7 | 🟠 مهم | خصوصية | بيانات شخصية (هوية/جوال/قضايا) بلا سياسة احتفاظ ولا مسح شامل | `js/storage.js` + `index.html` → زر «مسح كل الأرشيف»، حذف تلقائي بعد 30 يوماً عبر `timestamp` الموجود، وتنبيه «تُحفظ على هذا الجهاز فقط» | يقلّل نافذة تعرّض بيانات المستفيدين على أجهزة العمل المشتركة |
| 8 | 🟠 مهم | موثوقية | `sw.js` يمرر `undefined` لـ `respondWith()` عند فقد النسخة المخزّنة → `TypeError` | `sw.js` → `?? new Response('غير متاح دون اتصال', { status: 503 })` | خطأ أوفلاين مفهوم بدل انهيار طلب خام |
| 9 | 🟠 مهم | إتاحة وصول | `aria-pressed="true"` يبقى على المواد بعد إلغاء التحديد بـ «مسح» | `js/app.js` → أضف `el.setAttribute('aria-pressed','false')` في حلقة `clearInput()` | يزيل تضارباً بين ما يُرى وما يُنطق |
| 10 | 🟡 تحسين | أداء | Service Worker شبكة-أولاً لكل شيء → كل زيارة تنتظر الشبكة | `sw.js` → cache-first للأصول الثابتة، stale-while-revalidate لـ `data/*.json` و`index.html` | إقلاع فوري على شبكات المحاكم البطيئة مع بقاء التحديث |
| 11 | 🟡 تحسين | أداء / سلامة | 359 كائن RegExp يُبنى في كل نقرة «تحسين الصياغة» (قيس: 21ms — مقبول اليوم) + خطر تسلسل الاستبدالات | `js/response.js` → تعبير واحد بتبادل `(a|b|c)` مع دالة استبدال، يُبنى مرة واحدة | يلغي خطر التسلسل بنيوياً ويثبّت الأداء مع نمو القاموس |
| 12 | 🟡 تحسين | أداء | `findRelevantArticles` يعيد تطبيع نصوص كاملة داخل حلقة متداخلة (~880 عملية/تحليل) | `js/data.js` → احسب `_normTitle`/`_normText`/`_normKeywords` مرة في `loadData()` | يحافظ على سرعة التحليل عند نمو قاعدة المواد |
| 13 | 🟡 تحسين | اختبارات | صفر تغطية لـ `ui.js`/`admin.js`/`app.js` — وهي مكان كل الأخطاء الثلاثة أعلاه | أضف `jsdom` واختبر `renderArticles` (التهريب و`aria-pressed`) و`openModal/closeModal` (حبس التركيز) | خمسة اختبارات كانت ستلتقط بنود 2 و3 و9 قبل وصولها للإنتاج |
| 14 | 🟡 تحسين | تكرار | منطق تصدير Blob مكرر حرفياً في `app.js` و`admin.js`؛ و`safeSetItem`/`persist` مكرران | `js/ui.js` → `downloadJson(data, prefix)`؛ و`js/safe-storage.js` مشتركة | يشيل ~20 سطراً مكرراً ويوحّد مسار التصدير |
| 15 | 🟡 تحسين | العربية | ثلاثة أنظمة ترقيم/تقويم معاً: هجري بأرقام هندية + أرقام غربية + اسم ملف ميلادي | وحدة `format.js` بدالة `formatDate()` موحّدة، و`Intl.NumberFormat('ar-SA')` للعدّادات، وتاريخ هجري في اسم الملف المصدَّر | عرض عربي متسق؛ ملفات مصدَّرة يسهل ربطها بالسجلات |
| 16 | 🟡 تحسين | اتساق | `confirm()` الأصلي في 4 مواضع رغم وجود نظام Modal كامل | `js/ui.js` → `confirmDialog(message)` يرجع `Promise<boolean>` بنفس نمط النوافذ القائمة | تجربة موحّدة وRTL صحيح في التأكيدات |
| 17 | 🟡 تحسين | معمارية | ازدواج ملكية الحالة بين `app.js` و`admin.js` عبر دورة `onChange → setAdminState` | وحدة `js/store.js` بنمط اشتراك (`subscribe`) ومصدر حقيقة واحد | يزيل الدورة و~20 سطر تزامن؛ يسهّل إضافة أي مستهلك جديد |
| 18 | 🟡 تحسين | متانة | لا حد لحجم الملف المستورد (أرشيف أو مواد) | `js/app.js` + `js/admin.js` → `if (file.size > 5*1024*1024) { showToast('الملف أكبر من 5 ميجا'); return; }` | يمنع تجميد الواجهة بملف ضخم |
| 19 | 🟡 تحسين | أمن | `template.id` و`response.id` يُحقنان بلا `escapeHtml` (محميان اليوم باعتماد ضمني) | `js/ui.js` → لفّهما بـ `escapeHtml()` | يزيل اعتماداً ضمنياً بتكلفة صفرية |
| 20 | 🟡 تحسين | أمن | تعذّر `frame-ancestors` عبر `<meta>` على GitHub Pages | حارس سطر واحد: `if (window.top !== window.self) window.top.location = window.self.location;` | يسدّ clickjacking نظرياً حتى النقل لاستضافة تدعم الترويسات |
| 21 | 🟡 تحسين | محتوى | `lastVerified` مخزّن في كل مادة لكنه **غير معروض في الواجهة إطلاقاً** | `js/ui.js` → اعرضه كشارة صغيرة في بطاقة المادة، ونبّه بصرياً إذا تجاوز 12 شهراً | في أداة قانونية، عمر التحقق من المادة معلومة تخص الموظف مباشرة |
| 22 | 🟡 تحسين | نظافة | 11 مدخلاً ميتاً في `colloquial-map.json` (مفتاح = قيمة) | احذف: `سمعت`, `سمعنا`, `يسمع`, `يعطي`, `تعطي`, `أعطي`, `آخذ`, `يقول`, `تقول`, `أقول`, `جلستي` | يقلّل 11 RegExp وتمريرة نص بلا أي فقد وظيفي |
| 23 | 🟡 تحسين | UX | لا حالة تحميل أثناء `loadData()` — الصفحة تظهر فارغة على اتصال بطيء | `index.html` + `js/app.js` → هيكل عظمي بسيط أو `aria-busy="true"` حتى انتهاء `start()` | يزيل انطباع «التطبيق معطّل» على الشبكات البطيئة |

---

## ملحق — منهجية التحقق

كل ملاحظة سلوكية في هذا التقرير أُثبتت بتشغيل فعلي، لا بقراءة الكود وحدها:

- **بيئة التشغيل:** Chromium (Playwright) على خادم محلي `python3 -m http.server`، مقاس 1400×1000 لسطح المكتب و390×844 للجوال
- **ما قيس فعلياً:** أبعاد العناصر عبر `getBoundingClientRect()`، الأنماط المحسوبة عبر `getComputedStyle()`، نسب التباين بحساب `relative luminance` وفق WCAG، زمن التنفيذ عبر `performance.now()`، وحالة الـ DOM قبل/بعد كل إجراء
- **أخطاء الطرفية أثناء كل السيناريوهات:** **صفر** — لا `console.error` ولا استثناء غير ملتقط في أي مسار جُرّب
- **الاختبارات:** `npm test` → 29/29 ناجح
- **لقطات الشاشة:** التُقطت لتوثيق تداخل أيقونة البحث والعرض على الجوال
- **ملاحظة على النزاهة:** ظهر في القياس الآلي أن `.modal-close` بمقاس `0×0`، وتبيّن أن السبب أن النافذة كانت مغلقة وقت القياس لا عطلاً فعلياً — فاستُبعد ولم يُدرج كملاحظة

---

## 7. حالة التنفيذ — ✅ الخطة منفّذة بالكامل (23/23)

نُفّذت كل بنود الجدول أعلاه في نفس الفرع. الجدول التالي يربط كل بند بموضع تنفيذه وبالتحقق الذي أثبت إغلاقه.

| # | البند | الملفات | التحقق |
|---|---|---|---|
| 1 | تداخل أيقونة البحث | `css/styles.css` — `padding-inline` و`inset-inline-start` | بداية النص 620 مقابل حافة الأيقونة 624 → **لا تداخل** في كلتا الخانتين |
| 2 | ضياع نتائج التحليل | `js/app.js` — `app.lastRelevantArticles` | قبل: 10 → 44 مادة. بعد: **10 → 10** مع بقاء شارات التطابق |
| 3 | مسح الرد بلا تراجع | `js/app.js` — كل المسارات تمر بـ `setOutput()` | المسح يُظهر زر التراجع، والضغط عليه **يستعيد النص** |
| 4 | أسماء مُتاحة | `index.html` — `aria-label` + `aria-labelledby` | عناصر بلا اسم مُتاح: **صفر** |
| 5 | أهداف اللمس | `css/styles.css` | أزرار الأرشيف **44×44** (كانت 20×19)، والنص يبتعد عنها 16px |
| 6 | التباين | `css/themes.css` — `--text-light: #6b6b6b` | **5.06 : 1** (كان 3.28) — يتجاوز حد AA |
| 7 | سياسة الخصوصية | `js/storage.js` + `index.html` | تنبيه في الواجهة + اختيار مدة (٧/٣٠/٩٠/بلا حد) + **مسح كل الأرشيف** |
| 8 | `respondWith(undefined)` | `sw.js` — `offlineResponse()` | اختبار وحدة يثبت **503 بدل undefined** لكل من البيانات والمستند |
| 9 | `aria-pressed` عالق | `js/app.js` — `clearInput()` | بعد المسح: **`aria-pressed="false"`** |
| 10 | استراتيجية التخزين | `sw.js` | cache-first للأصول + SWR للبيانات؛ **إقلاع كامل والخادم مطفأ** |
| 11 | بناء 359 RegExp | `js/response.js` — تعبير واحد بتبادل + `WeakMap` | **21.2ms ← 1.9ms** على نفس النص |
| 12 | تطبيع مكرر | `js/data.js` — `withNormalized()` | التحليل الكامل **0.1ms** |
| 13 | تغطية الـ DOM | `tests/ui.test.js` + `sw.test.js` + `store.test.js` + `format.test.js` | **29 ← 81 اختباراً**، كلها ناجحة |
| 14 | تكرار الكود | `js/safe-storage.js` + `downloadJson()` | مسار تصدير واحد وغلاف تخزين واحد |
| 15 | التواريخ والأرقام | `js/format.js` | العدّادات هندية (`٤٤`)، التواريخ هجرية مثبّتة، اسم الملف **هجري+ميلادي** |
| 16 | `confirm()` الأصلي | `js/ui.js` — `confirmDialog()` | النافذة المخصصة تظهر و**النظام الأصلي لا يُستدعى** |
| 17 | ازدواج ملكية الحالة | `js/store.js` | مصدر واحد بنمط `subscribe`؛ دورة `onChange → setAdminState` أُزيلت |
| 18 | حجم الملف المستورد | `js/app.js` + `js/admin.js` | حد 5 ميجابايت في كلا مسارَي الاستيراد |
| 19 | تهريب المعرّفات | `js/ui.js` | اختبار يثبت أن `id` خبيث **لا يخرج من الخاصية** |
| 20 | حارس الإطار | `js/app.js` | حارس clickjacking عند تحميل الوحدة |
| 21 | ختم التحقق | `js/ui.js` + `css/styles.css` | **10 شارات** ظاهرة، وتتحول تحذيرية بعد 12 شهراً |
| 22 | مدخلات ميتة | `data/colloquial-map.json` | **344 ← 333** مدخلاً، بلا أي تغيير وظيفي |
| 23 | حالة التحميل | `index.html` + `css/styles.css` | شريط تحميل وهياكل عظمية، و`aria-busy` يُضبط ثم يُرفع |

### أخطاء اكتُشفت أثناء التنفيذ نفسه

**(أ) خطأ في سياسة الاحتفاظ التقطه اختبار جديد:** الصيغة الأولى قرأت التفضيل بـ `Number(readString(key, ''))`، و`Number('') === 0`، و`0` خيار صالح يعني «بلا حد». فالتفضيل **غير المضبوط** كان يُقرأ كإلغاء صريح للسياسة — أي أن الحذف التلقائي ما كان ليعمل لأي مستخدم لم يلمس الإعداد، وهم كل المستخدمين. أُصلح بفحص السلسلة قبل التحويل.

**(ب) `toLocaleDateString('ar-SA')` غير حتمي عبر البيئات:** أعطى **هجرياً** في Chromium و**ميلادياً** في Node على نفس السطر — لأن النتيجة تعتمد على بيانات ICU في البيئة لا على الشيفرة. هذا يوسّع البند 15: المشكلة لم تكن اختلاف أنظمة العرض فقط، بل أن التقويم المعروض نفسه غير مضمون. `js/format.js` يثبّت `ar-SA-u-ca-islamic-umalqura` صراحةً.

**(ج) تلوث الملف المصدَّر:** إضافة الحقول المطبّعة (البند 12) كانت ستُدرج `_normTitle` و`_normText` في ملف تصدير المواد. أُضيفت `toPlainArticle()` لتجريدها، مع اختبار يحرس ذلك.

### قياسات ما بعد التنفيذ

| المقياس | قبل | بعد |
|---|---|---|
| الاختبارات | 29 | **81** ✅ |
| الوحدات المغطاة | 4 من 9 | **9 من 11** (`app.js` و`theme.js` فقط بلا تغطية) |
| `improveLanguage` (~2.5 ألف حرف) | 21.2 ms | **1.9 ms** |
| تباين `--text-light` (فاتح) | 3.28 : 1 ❌ | **5.06 : 1** ✅ |
| أصغر هدف لمس | 20×19 px ❌ | **44×44 px** ✅ |
| عناصر بلا اسم مُتاح | 3 | **0** ✅ |
| ثغرات `npm audit` | 2 (خطورة عالية) | **0** ✅ |
| أخطاء طرفية | 0 | **0** ✅ |

> ثغرتا `npm audit` كانتا في اعتماديات تطوير غير مباشرة موجودة قبل هذا العمل (من شجرة ESLint)، وأُغلقت مع تحديث ملف القفل.

### ما لم يتغير عمداً

- **دقة المحتوى النظامي** — لم تُمسّ نصوص المواد الـ44 ولا أرقامها. تعديلها يحتاج مراجعة قانونية مختصة لا هندسية.
- **جودة الردود المولّدة ودقة المطابقة** — تحتاج اختباراً ميدانياً على رسائل حقيقية، والخطة المعتمدة لم تتضمنها.
- **ألوان النبرات الثابتة** (`#c0392b` وأخواتها) لا تتبدّل في الوضع الداكن — ملاحظة وردت في القسم 3.1 لكنها **لم تكن ضمن جدول البنود الـ23 المعتمد**، فتُركت لقرار لاحق.

---

*صدر عبر مهارة **مِحَك** — 15 أغسطس 2026*
